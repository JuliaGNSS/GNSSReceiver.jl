var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/#API-Reference","page":"API Reference","text":"","title":"API Reference"},{"category":"section","location":"api/#Running-the-receiver","page":"API Reference","text":"","title":"Running the receiver"},{"category":"section","location":"api/#Signals","page":"API Reference","text":"","title":"Signals"},{"category":"section","location":"api/#Reading-and-recording-samples","page":"API Reference","text":"The lower-level write_to_file (write raw samples straight to disk) is re-exported from SignalChannels; see that package for its documentation.","title":"Reading and recording samples"},{"category":"section","location":"api/#Hardware-correlators","page":"API Reference","text":"See the hardware-correlator contract for what a device package and its loop process have to satisfy.","title":"Hardware correlators"},{"category":"section","location":"api/#Capabilities","page":"API Reference","text":"","title":"Capabilities"},{"category":"section","location":"api/#RF-bands,-inputs-and-the-receiver-timebase","page":"API Reference","text":"","title":"RF bands, inputs and the receiver timebase"},{"category":"section","location":"api/#The-delay-aware-tracking-loop","page":"API Reference","text":"The estimator a hardware correlator's loops run — NCOReferencedPLLAndDLL, its per-satellite state, the NCOTimeline of what a device NCO ran, and the per-record step_loop that attributes a record to the word it really ran under — lives in TrackingLoops.jl and is stepped by the loop process. Tracking.jl re-exports it, and the software receiver runs it with the chunk's own replica word and no landing sample, which makes it the conventional loop to the bit. Its documentation is TrackingLoops'.","title":"The delay-aware tracking loop"},{"category":"section","location":"api/#Consuming-the-results","page":"API Reference","text":"","title":"Consuming the results"},{"category":"section","location":"api/#Data-types","page":"API Reference","text":"","title":"Data types"},{"category":"section","location":"api/#Lock-detection","page":"API Reference","text":"","title":"Lock detection"},{"category":"section","location":"api/#Beamforming","page":"API Reference","text":"","title":"Beamforming"},{"category":"section","location":"api/#Internals","page":"API Reference","text":"","title":"Internals"},{"category":"function","location":"api/#GNSSReceiver.receive","page":"API Reference","text":"receive(measurement_channel, systems, sampling_freq; num_ants = NumAnts(1), kwargs...)\nreceive(measurement_channels::Tuple, systems_per_band::Tuple, sampling_freq; kwargs...)\n\nRun the full acquire → track → decode → PVT pipeline over the samples arriving on measurement_channel and return a channel of per-chunk ReceiverDataOfInterest.\n\nsystems is a single GNSS system, a CombinedSignal pilot+data pair, or a tuple of these that share one RF band and sample stream; every constellation is acquired, tracked and decoded, and all are fused into a single multi-GNSS PVT solution. The multi-band method takes a tuple of measurement channels (one per RF band), a tuple of per-band system groups and a tuple of interm_freqs, all aligned band-by-band, and fuses every band into one solution with per-constellation clock biases and per-band inter-frequency biases. The band channels must deliver frames of equal duration from one shared time base (e.g. a single capture split by front-end channel) so one frame per band stays aligned each step; the first band is the receiver's clock. sampling_freq is one frequency for every band — the usual case of one sample clock feeding the front end — or a tuple aligned with the bands, for a front end whose bands genuinely run at different rates, in which case the frames differ in length but not in span. The number of antenna channels in each SignalChannel must equal N in num_ants.\n\nSampled at sampling_freq, each chunk is processed by process in a spawned task; one ReceiverDataOfInterest is emitted per pvt_update_interval. Acquisition (Acquisition 2 FM-DBZP with CFAR) runs at most every acquire_every; its coherent length and Doppler resolution are derived per system from the tracking loop pull-in range, and prns restricts the search (nothing ⇒ each constellation's default range, a per-GNSS NamedTuple/Dict keyed by get_constellation_id, or a plain collection applied to every system). A satellite is declared locked from its CN0 — referred to a one-code-block record, so an N-block record locks at 10·log10(N) dB less — and contributes to the PVT solution — recomputed every pvt_update_interval — after time_in_lock_before_calculating_pvt. enable_ionospheric_correction, enable_tropospheric_correction and pvt_approximate_year (which resolves the GPS week-number rollover for old recordings) are passed through to calc_pvt.\n\nPass vector_tracking = true to switch to vector tracking: once a first scalar fix is available, a navigation Kalman filter closes every satellite's code/carrier loop centrally from the fused multi-GNSS solution instead of per-satellite loop filters, and the emitted PVT solutions come from the filter. Like the scalar solve, the filter estimates one clock bias per GNSS time system and one inter-frequency bias per band beyond a reference band, and corrects the pseudoranges for the broadcast ionospheric model and the tropospheric delay.\n\nPassing a VectorTracking in place of true enables vector tracking and configures the filter, which is worth doing whenever the platform or the front end is known: the defaults assume vehicular dynamics and a TCXO-grade oscillator, and both the dynamics (acceleration_noise_std) and the oscillator stability (h0, hm2) materially change how the filter weighs its prediction against the measurements. Each payload then reports what the filter is doing through its VTStatus.\n\nThe downconvert-and-correlator backend is auto-selected from the sample element type: Complex{Int16} inputs use Tracking's fast integer backend when max_meas (the front-end full-scale, e.g. 2^11 for a 12-bit ADC) is given, and otherwise fall back to the float CPU backend (logged once); every other element type uses the float CPU backend and ignores max_meas. Pass downconvert_and_correlator to override the choice.\n\n\n\n\n\nreceive(loop::RemoteHardwareLoop, systems, sampling_freq; kwargs...)\n\nRun the pipeline with the tracking loops in the loop process loop talks to. Acquisition, decoding and PVT run here off the device's raw stream(s); the loops' Dopplers, code phases, bits and C/N₀ are mirrored from the loop's events. Vector tracking closes the loops on the host and is refused. Every other keyword is receive's.\n\n\n\n\n\n","title":"GNSSReceiver.receive"},{"category":"function","location":"api/#GNSSReceiver.gnss_receiver_gui","page":"API Reference","text":"gnss_receiver_gui(; system = GPSL1CA(), sampling_freq = 2e6Hz, kwargs...)\n\nAcquire, track and compute a PVT solution from a live SoapySDR device and display the result in a live terminal GUI. When the stream ends the final frame stays on screen; the call blocks until you quit the dashboard with q.\n\nsystem is a single system or a tuple of systems sharing one RF band (see receive / CombinedSignal). The device selected by dev_args is configured for sampling_freq, tuned to the shared band's centre frequency and streamed for run_time — all handled by SignalChannels' stream_data, which buffers a few seconds of signal for acquisition headroom and rechunks to the chunk_time processing length. Set gain to a fixed value or leave it nothing for automatic gain control; antenna, interm_freq and num_ants override the RF front end and antenna-channel count (one identical channel config is used per antenna). Samples are streamed as stream_type (default ComplexF32); pass stream_type = Complex{Int16} to use Tracking's fast integer backend on an Int16-native device, in which case max_meas (the front-end full-scale) is required. It is ignored for float stream types.\n\n\n\n\n\n","title":"GNSSReceiver.gnss_receiver_gui"},{"category":"type","location":"api/#GNSSReceiver.ReceiverState","page":"API Reference","text":"ReceiverState(T, systems; num_samples_for_acquisition, num_ants = NumAnts(1), kwargs...)\n\nBuild the initial receiver state for tracking systems, where T is the element type of the incoming signal samples (e.g. ComplexF64 or Complex{Int16}).\n\nsystems is a single GNSS system, a CombinedSignal pilot+data pair, or a tuple of these sharing one RF band; each becomes a tracking group in a single TrackState, keyed by its ranging signal's id. num_samples_for_acquisition sizes the acquisition sample buffer, and num_ants selects single- versus multi-antenna processing. vector_tracking = true closes the tracking loops through a navigation filter instead of per-satellite loop filters (the tracking-loop estimator follows from this: the conventional FLL-assisted PLL/DLL for scalar, VectorPLLAndDLL for vector tracking); pass a VectorTracking instead of true to describe the platform's dynamics and the receiver's oscillator to that filter. One ReceiverState spans every band; pass the per-band system tuples and pre-built acquisition buffers to the primary constructor for the multi-band case.\n\n\n\n\n\n","title":"GNSSReceiver.ReceiverState"},{"category":"type","location":"api/#GNSSReceiver.CombinedSignal","page":"API Reference","text":"CombinedSignal(pilot, data)\n\nA pilot + data signal pair tracked jointly in one group off a shared carrier/code loop: the receiver ranges on the dataless pilot (its prompt has no navigation-bit transitions to squash the discriminator) and decodes the navigation message from the data component — e.g. CombinedSignal(GalileoE1C(), GalileoE1B()) or CombinedSignal(GPSL5Q(), GPSL5I()).\n\nPass it anywhere receive / ReceiverState take a signal system; a plain AbstractGNSSSignal (e.g. GPSL1CA()) is the data-only alternative.\n\n\n\n\n\n","title":"GNSSReceiver.CombinedSignal"},{"category":"function","location":"api/#GNSSReceiver.read_files","page":"API Reference","text":"read_files(files, num_samples, end_condition = nothing; type = Complex{Int16},\n           read_measurement! = read_measurement!)\n\nReturn a SignalChannel that yields num_samples-long chunks read from files, one file per antenna channel (a single path is treated as one channel). Samples are read as type elements.\n\nThe read runs on a spawned task that stops when end_condition is reached: an Integer sample count, a notified Base.Event, or end-of-file (nothing reads until EOF). Each chunk is a freshly allocated buffer, since the lock-free channel may still hold a previously enqueued chunk.\n\nA custom read_measurement!(streams, chunk) can be supplied to decode packed or otherwise-encoded sample formats; see the note above for the contract it must honour.\n\nPass sample_rate (the recording's sampling frequency) to replay in real time — chunks are then released on a fixed schedule, one chunk's worth of signal time (num_samples / sample_rate) apart, so the replay neither runs ahead nor drifts behind the pace the signal was recorded at. Left nothing, chunks are pushed as fast as the pipeline consumes them.\n\nPacing is bounded below by sleep's ~1 ms resolution: a chunk period much shorter than that cannot be tracked, and replay runs slower than real time regardless.\n\n\n\n\n\n","title":"GNSSReceiver.read_files"},{"category":"function","location":"api/#GNSSReceiver.read_uint8_iq_file","page":"API Reference","text":"read_uint8_iq_file(file, num_samples, end_condition = nothing; center = 128,\n                   type = Complex{Int16})\n\nReturn a SignalChannel that yields num_samples-long chunks read from a single raw recording of interleaved 8-bit unsigned offset-binary in-phase/quadrature samples — the native sample format of many SDRs (e.g. RTL-SDR) and of public GNSS sample sets such as the ION SDR test data. Each unsigned byte pair (I, Q) is recentred on center (128 for standard offset binary, where 128 ≈ zero) and returned as a baseband sample of element type type.\n\nThis is the single-file, offset-binary counterpart to read_files, which reads samples that are already stored in the target element type. Because the recentred samples satisfy |real|, |imag| ≤ center, pass max_meas = center to receive when type is Complex{Int16} (the default) so it selects Tracking's fast integer downconvert-and-correlator. For a float type (e.g. ComplexF32) use center = 127.5 for exact midscale recentring and drop max_meas.\n\nend_condition stops the read exactly like read_files: an Integer sample count, a notified Base.Event, or nothing to read until end-of-file.\n\nPass sample_rate (the recording's sampling frequency) to replay in real time: chunks are then released on a fixed schedule, one chunk's worth of signal time (num_samples / sample_rate) apart, so the file plays back at the pace it was recorded — useful for watching the live gui. Left nothing, chunks are pushed as fast as the pipeline consumes them. Pacing is bounded below by sleep's ~1 ms resolution (see read_files).\n\n\n\n\n\n","title":"GNSSReceiver.read_uint8_iq_file"},{"category":"function","location":"api/#GNSSReceiver.gnss_write_to_file","page":"API Reference","text":"gnss_write_to_file(; system = GPSL1CA(), sampling_freq = 2e6Hz, run_time = 4s,\n                   dev_args = first(Devices()), output_file = \"gnss_test_data\",\n                   gain = 50dB)\n\nStream run_time of raw Complex{Int16} samples from a SoapySDR device (tuned to the shared band's centre frequency, at gain or AGC when gain === nothing) to disk, for later offline replay with read_files and receive. SignalChannels' write_to_file names each antenna channel's file \"$(output_file)$(type)$(channel).dat\" — e.g. \"gnss_test_dataComplex{Int16}1.dat\" with the defaults — so pass that full name (not output_file itself) to read_files when replaying. Blocks until the recording finishes.\n\n\n\n\n\n","title":"GNSSReceiver.gnss_write_to_file"},{"category":"type","location":"api/#GNSSReceiver.AbstractHardwareCorrelatorSDR","page":"API Reference","text":"AbstractHardwareCorrelatorSDR\n\nA software-defined radio whose FPGA correlates and whose tracking loops are closed by a separate loop process. Subtype it to describe such a device to the receiver, then hand it to RemoteHardwareLoop and receive(loop, systems, sampling_freq).\n\nRequired: raw_sample_channel (the raw I/Q stream acquisition runs on; per band for a multi-band device) and num_hardware_channels. Declare what the gateware can do with hardware_capabilities — every configured signal is validated against it before anything is armed — and, for a multi-band device, the RF routing through band_rf_input, band_hardware_channels, band_device_index and clock_synchronization. correlator_gain and replica_code_amplitude state the replica amplitudes the loop divides out of every accumulator, and device_sample_origin maps host raw sample 0 onto the device counter, so an acquisition's code phase can be armed at the right device sample.\n\nThe device's records and NCO words never pass through this process: the loop process reads and writes them and publishes what the receiver needs — epoch states, records, bits, status — through the protocol segment.\n\n\n\n\n\n","title":"GNSSReceiver.AbstractHardwareCorrelatorSDR"},{"category":"type","location":"api/#GNSSReceiver.RemoteHardwareLoop","page":"API Reference","text":"RemoteHardwareLoop(sdr; segment, spawn = nothing, kwargs...)\n\nThe receiver's handle on a loop process serving the device sdr streams from.\n\nsegment is the protocol segment's path (/dev/shm/gnss-loop-<device>), or an already open HardwareLoopProtocol.Segment (a heap-backed one in the tests). Given a path: a live loop found there is attached to and told to release every channel; otherwise spawn (a Cmd starting the loop executable) is run and waited for, or an ArgumentError says there is nothing to attach to. A loop whose heartbeat stops for heartbeat_timeout seconds is restarted from spawn and every tracked satellite re-armed from the receiver's own state.\n\nsdr provides the raw streams (raw_sample_channel), the declared hardware_capabilities, the band plan and the device-counter origins (device_sample_origin).\n\n\n\n\n\n","title":"GNSSReceiver.RemoteHardwareLoop"},{"category":"method","location":"api/#GNSSReceiver.receive-Tuple{RemoteHardwareLoop, Any, Any}","page":"API Reference","text":"receive(loop::RemoteHardwareLoop, systems, sampling_freq; kwargs...)\n\nRun the pipeline with the tracking loops in the loop process loop talks to. Acquisition, decoding and PVT run here off the device's raw stream(s); the loops' Dopplers, code phases, bits and C/N₀ are mirrored from the loop's events. Vector tracking closes the loops on the host and is refused. Every other keyword is receive's.\n\n\n\n\n\n","title":"GNSSReceiver.receive"},{"category":"function","location":"api/#GNSSReceiver.device_sample_origin","page":"API Reference","text":"device_sample_origin(sdr::AbstractHardwareCorrelatorSDR, band_id::Symbol) -> Int64\n\nThe device counter reading, on band_id's counter, that corresponds to host raw sample 0 of that band — what an acquisition's valid_at_sample has to be offset by before it is handed to the loop process. The default is 0: device and host count the same stream from the same origin (true of the simulated device). A real device latches its counter when the raw stream starts.\n\n\n\n\n\n","title":"GNSSReceiver.device_sample_origin"},{"category":"function","location":"api/#GNSSReceiver.raw_sample_channel","page":"API Reference","text":"raw_sample_channel(sdr::AbstractHardwareCorrelatorSDR) -> SignalChannel\n\nThe device's raw sample stream. Required; see AbstractHardwareCorrelatorSDR.\n\n\n\n\n\nraw_sample_channel(sdr, band_id::Symbol) -> SignalChannel\n\nOne RF band's raw sample stream. The default is the device's single stream, so a one-band device implements nothing; a device that receives several bands at once returns a separate stream per band, each counted at that band's own band_sampling_frequency and each driving its own band's acquisition.\n\nThe streams stay separate rather than being interleaved for the same reason the dumps and the raw samples do: they have different rates, and the receiver's acquisition, buffering and code-phase correction are all per band already.\n\n\n\n\n\n","title":"GNSSReceiver.raw_sample_channel"},{"category":"function","location":"api/#GNSSReceiver.num_hardware_channels","page":"API Reference","text":"num_hardware_channels(sdr::AbstractHardwareCorrelatorSDR) -> Int\n\nHow many hardware tracking channels (replica sets) the gateware provides. Required; see AbstractHardwareCorrelatorSDR.\n\n\n\n\n\n","title":"GNSSReceiver.num_hardware_channels"},{"category":"function","location":"api/#GNSSReceiver.correlator_gain","page":"API Reference","text":"correlator_gain(sdr) -> Real\n\nAmplitude of the replica the device wipes the carrier off with, relative to the unit-amplitude replica a host correlator would use on the same samples. The ingest divides it out, so a satellite's prompt lands on the same scale as the raw samples it was correlated from.\n\nA device that mixes with a ±127 sine/cosine table returns 127; one that normalises in the gateware returns the default 1. It is a pure scale, so the discriminators and any moment-ratio C/N₀ estimator cannot see it — but Tracking's noise-referenced C/N₀ divides the prompt power by a floor measured from the raw samples, and there a wrong gain is a 20·log10(g) dB offset on every satellite. Getting it wrong is therefore visible only as a uniform C/N₀ bias, which is exactly the kind of error a lock-detector threshold silently absorbs, so declare it rather than leaving it at the default.\n\ncorrelator_gain(sdr, band_id) -> Real\n\nThe same thing for one RF band (GNSSSignals.get_band_id: :L1, :L5, …), defaulting to the device-wide value. A multi-band front end rarely has one scale: each band has its own gain chain and its own replica table, and a C/N₀ referenced to raw-sample power is biased by 20·log10(g) per band. Declare it per band and every band's satellites land on the same scale.\n\nRead once per assignment — the arm command carries the result as its replica amplitude — so a device may compute it rather than store it.\n\n\n\n\n\n","title":"GNSSReceiver.correlator_gain"},{"category":"function","location":"api/#GNSSReceiver.is_observation_gap","page":"API Reference","text":"is_observation_gap(correlator_source, track_state, group_key, prn) -> Bool\n\nWhether this chunk delivered no measurement at all for the satellite, in a way that says nothing about the signal — so the receiver freezes its lock detectors instead of letting them decay through it.\n\nfalse for a software correlator: there the chunk's samples are the measurement, and a chunk that produces no record produced none because the signal was not there. A RemoteHardwareLoop answers true for a satellite whose records were delayed on their way from the device, within a bounded gap — the device correlates whether or not the host is listening, and its own logs showed the FPGA holding satellites through gaps of seconds on the last NCO word (issue #107). Past the gap budget the path is not late but broken, and the detectors are allowed to decay so the satellite is eventually released rather than held in a lock nothing confirms.\n\nwarning: Wrappers must forward this\nA correlator source that wraps a loop — an instrumented source that forwards advance_tracking!, say — matches the fallback below, not the loop's method, and its satellites silently lose the protection. Forward is_observation_gap, has_current_observations and take_bit_clock_restart! explicitly.\n\n\n\n\n\n","title":"GNSSReceiver.is_observation_gap"},{"category":"function","location":"api/#GNSSReceiver.take_bit_clock_restart!","page":"API Reference","text":"take_bit_clock_restart!(correlator_source, group_key, prn) -> Bool\n\nWhether the source restarted this satellite's bit clock since the receiver last asked, clearing the flag. The receiver restarts the satellite's decoder in response, because the bit stream it was decoding has been cut. false for every source but a RemoteHardwareLoop whose loop process lost records.\n\n\n\n\n\n","title":"GNSSReceiver.take_bit_clock_restart!"},{"category":"function","location":"api/#GNSSReceiver.advance_tracking!","page":"API Reference","text":"advance_tracking!(correlator_source, band_measurements, track_state, band_systems) -> TrackState\n\nAdvance track_state by one processing chunk and return it. This is the single point where the software and hardware-correlator receivers differ; everything around it — acquisition, lock detection, decoding, PVT — is shared.\n\nThe software method takes any Tracking downconvert-and-correlator backend and simply calls track!, which correlates the raw chunk itself. The RemoteHardwareLoop method ignores the samples for tracking purposes (the FPGA already correlated them and the loop process closed the loops) and instead mirrors the loop process's events into the tracking state.\n\n\n\n\n\n","title":"GNSSReceiver.advance_tracking!"},{"category":"type","location":"api/#GNSSReceiver.HardwareCorrelatorCapabilities","page":"API Reference","text":"HardwareCorrelatorCapabilities\n\nWhat a hardware correlator's gateware can replicate and correlate — the vendor-neutral answer to \"may this device be asked to track this signal?\".\n\nEvery field is a limit, not a promise about a particular channel, and every one of them can be left unrestricted. Build one with keywords:\n\nHardwareCorrelatorCapabilities(;\n    signals = [:GPSL1CA, :GalileoE1B],      # `GNSSSignals.get_signal_id`s, or `nothing` for any\n    modulations = [:LOC, :CBOC],            # `nameof(typeof(get_modulation(signal)))`, or `nothing`\n    max_primary_code_length = 4092,         # chips the code memory holds\n    code_frequency_limits = (1.023e6, 1.023e6),  # Hz, inclusive\n    tap_layouts = [3, 5],                   # accumulator counts the correlator bank can produce\n    max_tap_offset_chips = 1.0,             # how far from prompt a replica can be placed\n    num_antennas = 1,\n    bands = [:L1],                          # `GNSSSignals.get_band_id`s, or `nothing` for any\n    num_rf_inputs = 1,                      # bands that can be received at once\n    max_secondary_code_length = 1,          # longest overlay the gateway can wipe off; 1 = none\n    reports_code_phase = true,              # does it latch the replica code phase per record?\n    supports_partial_code_dumps = false,    # can it dump *inside* a code period?\n)\n\nFields:\n\nsignals — the get_signal_id symbols the gateware has code generators for, or nothing when it can replicate any signal GNSSSignals describes (a device that loads code tables from the host, say).\nmodulations — the modulation type names it can synthesise (:LOC, :BOCsin, :BOCcos, :CBOC, :TMBOC), or nothing for any. Declaring this separately from signals is what lets a device that has a generic BPSK generator say so without enumerating every BPSK signal.\nmax_primary_code_length — chips of primary code the channel's code memory holds. GPS L2 CL's 767 250-chip code is the one that fails this on almost every device; see issue #133 for what a device short of it can still do.\ncode_frequency_limits — inclusive (min, max) chip rate in Hz the code NCO covers.\ntap_layouts — the numbers of accumulators one channel can produce. [3] is an E/P/L bank, [3, 5] one that can also do VE/E/P/L/VL. A signal whose Tracking correlator has a tap count that is not in this list cannot be tracked on this device: the missing taps cannot be invented host-side.\nmax_tap_offset_chips — how far either side of prompt a replica can be placed, in chips. The LiteX-M2SDR's code RAM reaches ±1 chip and rejects anything wider.\nnum_antennas — accumulators per tap, i.e. how many antenna chains the correlator bank despreads in parallel. Beamforming is post-correlation on the host, so this is what caps EigenBeamformer's array.\nbands — the get_band_id symbols the RF front end can tune, or nothing for any.\nnum_rf_inputs — how many of those bands can be received at the same time. Simultaneous all-band reception is an RF-capacity constraint, not a correlator one, and this is where it is declared.\nmax_secondary_code_length — the longest secondary (overlay) code the gateware can wipe off itself, or 1 for \"primary code only\". Purely a declaration: the loop process removes the overlay from the records itself once the sync detector has found its phase, so nothing asks a device to do it and 1 costs a device nothing. It is what supports_secondary_code_wipeoff reads, and what a future scheduled wipeoff contract — which would let a device pre-accumulate across code periods — would gate on.\nreports_code_phase — whether the device latches the replica's code phase alongside the accumulators (the record's code phase). Informational: without it pseudoranges are dead-reckoned from the handover seed rather than anchored to the replica the DLL steers, and a partial-primary record stream has to dead-reckon its place on the code-block grid from the handover instead of reading it off every record.\nsupports_partial_code_dumps — whether a channel can be told to dump inside a primary code period rather than only on the code wrap. false is the historical contract (one record per code period) and is what every device declares until issue #133's steps 2 and 3 land.\nThis is a hard requirement rather than a nicety for a long code: GPS L2CL's 767 250 chips at 511.5 kcps are a 1.5 s period, so a device that only dumps on the wrap hands the tracking loops one record and one NCO correction every 1.5 s. hardware_support_error refuses that combination before a channel is armed — see its max_integration_time keyword for where the line is drawn. What the loop process does with a device that can dump inside a code period is HardwareLoopCore's record accounting.\n\nA device declares its own with hardware_capabilities; one that does not is taken to be LEGACY_GPS_L1CA_CAPABILITIES.\n\n\n\n\n\n","title":"GNSSReceiver.HardwareCorrelatorCapabilities"},{"category":"constant","location":"api/#GNSSReceiver.LEGACY_GPS_L1CA_CAPABILITIES","page":"API Reference","text":"LEGACY_GPS_L1CA_CAPABILITIES\n\nThe profile of a device that predates hardware_capabilities: the GPS L1 C/A correlator the hardware path was written against (issues #107, #129). One RF input on L1, a 1023-chip BPSK code at 1.023 Mcps, a three-tap E/P/L bank reaching ±1 chip, one antenna, no secondary-code wipeoff.\n\nThis is what hardware_capabilities returns for a device that declares nothing, so an existing adapter keeps working and keeps being described truthfully — and a request it cannot serve is refused with an actionable error instead of reaching the gateware.\n\n\n\n\n\n","title":"GNSSReceiver.LEGACY_GPS_L1CA_CAPABILITIES"},{"category":"function","location":"api/#GNSSReceiver.hardware_capabilities","page":"API Reference","text":"hardware_capabilities(sdr) -> HardwareCorrelatorCapabilities\n\nWhat sdr's gateware can replicate and correlate. Optional; the default is LEGACY_GPS_L1CA_CAPABILITIES, i.e. \"the GPS L1 C/A device this interface was written against\".\n\nDeclare it as soon as a device does anything else — the receiver validates every configured signal against it before it arms a channel (validate_hardware_configuration), so an undeclared capability is a capability the receiver will refuse to use, and an over-declared one is a channel that never locks.\n\n\n\n\n\n","title":"GNSSReceiver.hardware_capabilities"},{"category":"function","location":"api/#GNSSReceiver.validate_hardware_configuration","page":"API Reference","text":"validate_hardware_configuration(sdr, systems, sampling_freq; num_ants) -> nothing\n\nCheck every signal the receiver would track against sdr's declared hardware_capabilities and throw one ArgumentError listing every problem — before a channel is armed, before a single CSR is written.\n\nsystems is what receive was given (a signal, a CombinedSignal, a tuple of them sharing one band, or a tuple of such tuples, one per band); each system's components are checked one by one against its own band's sampling frequency, so a pilot/data pair is accepted only if the device can serve both. The device's dump record is checked too: a three-slot record cannot carry a five-tap correlator, which is the failure this validation exists to replace — at PR #129's head that combination reached the ingest path and died there as a DimensionMismatch (issue #131).\n\nsampling_freq is one frequency for every band, or a tuple aligned with the band groups. band_plan is the RF configuration to check (hardware_band_plan builds the device's own); its bands, RF inputs, devices and clock relationship are validated as a whole, because receiving every requested band at once is an RF-capacity question the per-signal checks cannot answer (see band_plan_error).\n\nThis is the pre-arm gate; receive(::AbstractHardwareCorrelatorSDR, …) calls it for you. Call it directly when building a link by hand.\n\n\n\n\n\n","title":"GNSSReceiver.validate_hardware_configuration"},{"category":"function","location":"api/#GNSSReceiver.check_hardware_support","page":"API Reference","text":"check_hardware_support(sdr, signal, sampling_freq; correlator, num_ants, dump_tap_slots) -> nothing\n\nThrow an ArgumentError naming sdr and every reason it cannot track signal at sampling_freq, or return nothing when it can. The single-signal form of validate_hardware_configuration.\n\ncorrelator defaults to the one Tracking tracks signal with; pass it when the receiver is configured with another.\n\n\n\n\n\n","title":"GNSSReceiver.check_hardware_support"},{"category":"function","location":"api/#GNSSReceiver.hardware_support_error","page":"API Reference","text":"hardware_support_error(capabilities, signal, correlator, sampling_freq;\n                       num_ants, dump_tap_slots) -> Union{Nothing,String}\n\nEvery reason capabilities cannot serve signal tracked with correlator at sampling_freq, as one message — or nothing when it can.\n\ncorrelator is the host-side Tracking correlator the signal is tracked with (Tracking.get_default_correlator(signal, num_ants) unless overridden): its tap count and preferred shifts are what the device must reproduce, so they are what is checked. dump_tap_slots, when given, is how many accumulator slots the device's dump record carries — a wire too narrow for the correlator cannot transport it, which is the same refusal one step earlier in the path.\n\nmax_integration_time is the longest span of signal the receiver will fold into one record (the loop process's max_integration_time, and DEFAULT_MAX_INTEGRATION_TIME here). It is what decides whether the signal's primary code period can be the unit of integration: past it a record has to be cut inside a code period, which a device that only dumps on the code wrap cannot do. That is the one check here about timing rather than replicas, and it is the reason a 1.5 s GPS L2CL code is refused on a device that has the code memory for it but dumps once per period.\n\nAll reasons are collected rather than reported one at a time: a message that stops at the first one sends the reader round the loop for each of the rest.\n\n\n\n\n\n","title":"GNSSReceiver.hardware_support_error"},{"category":"function","location":"api/#GNSSReceiver.wire_tap_slots","page":"API Reference","text":"wire_tap_slots(::Type{<:Tracking.AbstractCorrelator}) -> Union{Nothing,Int}\n\nHow many accumulator slots a dump record built on this correlator type carries, known from the type alone — before any dump has arrived, and without an instance to take a length of. nothing for a correlator type this package does not know the width of, which simply skips the wire-width check.\n\nA device's record format fixes one such width for the whole run, and it has to be wide enough for every correlator the receiver tracks with: a three-slot record cannot carry a five-tap correlator, and the loop will not invent the missing taps.\n\n\n\n\n\n","title":"GNSSReceiver.wire_tap_slots"},{"category":"constant","location":"api/#GNSSReceiver.DEFAULT_MAX_INTEGRATION_TIME","page":"API Reference","text":"DEFAULT_MAX_INTEGRATION_TIME\n\nThe longest span of signal the hardware path folds into one correlator record by default, in seconds: 20 ms, one GPS navigation bit.\n\nIt is the line between a signal whose primary code period can be the unit of integration and one whose cannot. Every signal in issue #130's scope but GPS L2CL has a code period at or below it — 1 ms for GPS L1 C/A and L5, 4 ms for Galileo E1, 20 ms for GPS L2CM, 64.5 µs for Galileo E5a-QP — and for those the records the loops are handed are whole code blocks, which is what keeps the navigation bit grid and the overlay counter exact. L2CL's 1.5 s period is three orders of magnitude past it, so its records are partial: cut inside a code period, counted as the fraction of one they are, and never as a completed period.\n\nBoth the loop process (its LoopConfig.max_integration_time) and the pre-arm gate (hardware_support_error) read it, so a device is refused for exactly the signals the configured integration length cannot serve.\n\n\n\n\n\n","title":"GNSSReceiver.DEFAULT_MAX_INTEGRATION_TIME"},{"category":"function","location":"api/#GNSSReceiver.replica_code_amplitude","page":"API Reference","text":"replica_code_amplitude(sdr, signal) -> Real\n\nPer-sample RMS amplitude of the code replica sdr's gateware correlates signal with, on the same scale GNSSSignals.get_code_amplitude reports for the host's own table.\n\nThe default is get_code_amplitude(signal): the device reproduces the modelled code exactly, which is true for every ±1 code (BPSK, BOC, TMBOC) and is the only case the legacy L1 C/A path had. It is not true where the gateware approximates — a device that replicates Galileo E1B with a plain ±1 BOC(1,1) replica has a code amplitude of 1 where GNSSSignals' multi-level CBOC table has ≈ 19.92 — and there the ingest has to rescale, or the same satellite reads ~26 dB apart depending on which correlator produced it.\n\nThis is a pure amplitude convention: the ingest divides every accumulator by replica_code_amplitude(sdr, signal) / get_code_amplitude(signal), so the prompt reaching Tracking is always on the host table's scale and Tracking.normalize's own division by get_code_amplitude lands on a modulation-independent, unit-power amplitude. It says nothing about the shape of an approximated correlation function; which approximations are usable at all is issue #135's matrix.\n\n\n\n\n\n","title":"GNSSReceiver.replica_code_amplitude"},{"category":"function","location":"api/#GNSSReceiver.supports_secondary_code_wipeoff","page":"API Reference","text":"supports_secondary_code_wipeoff(capabilities, signal) -> Bool\n\nWhether the device can wipe signal's secondary (overlay) code off in the gateware, so consecutive dumps can be summed without the overlay cancelling them.\n\nfalse — the default for every device — is not an error, and true changes nothing today: the loop process removes the overlay from each primary-period record once it knows its phase, so every arm command asks for :primary_only whatever a device declares. The \"Secondary codes\" section of the hardware-correlator contract says why ownership sits there, and what the removal unlocks.\n\n\n\n\n\n","title":"GNSSReceiver.supports_secondary_code_wipeoff"},{"category":"type","location":"api/#GNSSReceiver.HardwareBandRoute","page":"API Reference","text":"HardwareBandRoute(band_id; rf_input, device_index, sampling_freq)\n\nWhere one RF band physically arrives and how fast it is counted.\n\nband_id — the GNSSSignals.get_band_id symbol (:L1, :L5, :B1I, …). One route per band; a band is what a front end tunes.\nrf_input — the device's RF input (tuner / downconversion chain) this band comes in on, 1-based. Two independently tuned bands need two inputs, and the pre-arm validation refuses a plan that puts two bands on one.\ndevice_index — which physical device, 1-based, for a multi-device array. Anything past 1 needs a declared clock relationship (see HardwareBandPlan).\nsampling_freq — the rate, in Hz, this band's raw samples and its correlator dumps' sample_index are counted at. It need not equal any other band's.\n\nnote: An RF input is not an antenna\nrf_input selects a band; HardwareCorrelatorCapabilities.num_antennas counts the coherent chains that band is received on. An N-antenna L1 front end is one route with num_antennas = N, not N routes: the antennas share one LO and one sample clock, and the correlator bank despreads all of them into one SVector{N,Complex} accumulator per tap so that the host can beamform from the prompt covariance. Two bands share nothing but the board.\n\n\n\n\n\n","title":"GNSSReceiver.HardwareBandRoute"},{"category":"type","location":"api/#GNSSReceiver.HardwareBandPlan","page":"API Reference","text":"HardwareBandPlan(routes; clock_synchronization = :single_device)\n\nThe receiver's RF configuration: one HardwareBandRoute per band, in order, plus the clock relationship between the devices they live on.\n\nThe first route's band is the reference band, and its sample counter is the receiver timebase. Every epoch boundary, every fold, every NCO landing sample and every code-phase reference epoch is expressed in reference-band samples; a record arriving on another band is mapped onto that axis by the exact ratio of the two rates (to_receiver_samples), and a command going the other way is mapped back (to_band_samples). Since the ratio is exact and both counters are derived from one hardware clock, the mapping is a scaling and not an estimate — which is what lets two bands at 4 and 5 MS/s close the same epoch at the same instant, and what makes the pseudoranges of both bands referenced to one common reception time.\n\nclock_synchronization says what makes that true across device_indexes:\n\n:single_device (the default) — one device, one sample clock. Nothing to synchronise.\n:shared_clock — several devices driven from one reference and one sample clock (a common 10 MHz plus a distributed sample-clock/PPS), with their sample counters aligned at start. This is the only multi-device configuration the receiver supports, because the timebase mapping above is a ratio with no offset term.\n:independent — devices on free-running clocks. Their counters drift against each other by parts per million, which is metres of pseudorange per second and no bounded relationship for the fold grid at all. The pre-arm validation refuses it rather than producing a fix nothing can vouch for; a receiver that needs it has to estimate and steer the inter-device offset first, which this package does not do.\n\n\n\n\n\n","title":"GNSSReceiver.HardwareBandPlan"},{"category":"function","location":"api/#GNSSReceiver.hardware_band_plan","page":"API Reference","text":"hardware_band_plan(sdr, band_ids, sampling_freqs) -> HardwareBandPlan\n\nThe device's RF configuration for the requested bands: one HardwareBandRoute per band, in the order given — so the first band is the reference band and its counter is the receiver timebase — plus the device's declared clock_synchronization.\n\nThe default builds it from band_rf_input and band_device_index, which is all a device usually has to declare. Override the whole function only where the routing cannot be expressed per band (a device that swaps inputs depending on the combination requested, say).\n\nsampling_freqs is aligned with band_ids; a single frequency applies to every band, which is the common case of one sample clock feeding several tuners.\n\n\n\n\n\n","title":"GNSSReceiver.hardware_band_plan"},{"category":"function","location":"api/#GNSSReceiver.band_rf_input","page":"API Reference","text":"band_rf_input(sdr, band_id) -> Int\n\nWhich RF input (tuner / downconversion chain) of the device band_id arrives on, 1-based. The default is 1, which is right for every single-band device.\n\nA device that can receive several bands at once must implement this: the receiver will not guess which tuner a band lands on, and refuses a plan that puts two bands on one input rather than configuring a subset in silence. An RF input is not an antenna — see HardwareBandRoute.\n\n\n\n\n\n","title":"GNSSReceiver.band_rf_input"},{"category":"function","location":"api/#GNSSReceiver.band_device_index","page":"API Reference","text":"band_device_index(sdr, band_id) -> Int\n\nWhich physical device of a multi-device array band_id arrives on, 1-based. The default is 1. Anything else needs a declared clock_synchronization.\n\n\n\n\n\n","title":"GNSSReceiver.band_device_index"},{"category":"function","location":"api/#GNSSReceiver.band_hardware_channels","page":"API Reference","text":"band_hardware_channels(sdr, band_id) -> AbstractVector{Int}\n\nWhich of the device's hardware channels can correlate band_id — its correlator bank for that band, as 1-based indices into num_hardware_channels.\n\nThe default is every channel, which is right for a single-band device and for a multi-band one whose bank can be pointed at any input. A device whose replica sets are wired to one downconversion chain each returns that chain's slice, and the loop then only ever hands a band's satellites a channel that can actually see it — the raw acquisition stream and the correlator bank that serves it are the same front end.\n\nGetting this wrong is not a subtle failure: a channel of the wrong bank correlates the other band's samples with this band's replica, produces records that never rise above the noise, and the satellite is dropped as if it had faded.\n\nThe returned ranges must not overlap between bands.\n\n\n\n\n\n","title":"GNSSReceiver.band_hardware_channels"},{"category":"function","location":"api/#GNSSReceiver.clock_synchronization","page":"API Reference","text":"clock_synchronization(sdr) -> Symbol\n\nHow the sample clocks of the devices this adapter drives relate to each other: :single_device (the default), :shared_clock or :independent. See HardwareBandPlan for what each obliges and why :independent is refused for a multi-device plan.\n\n\n\n\n\n","title":"GNSSReceiver.clock_synchronization"},{"category":"function","location":"api/#GNSSReceiver.reference_band","page":"API Reference","text":"reference_band(plan) -> Symbol\n\nThe band whose sample counter is the receiver timebase: the plan's first.\n\n\n\n\n\n","title":"GNSSReceiver.reference_band"},{"category":"function","location":"api/#GNSSReceiver.reference_sampling_frequency","page":"API Reference","text":"reference_sampling_frequency(plan) -> Float64\n\nThe receiver timebase's rate in Hz — the reference band's sampling frequency.\n\n\n\n\n\n","title":"GNSSReceiver.reference_sampling_frequency"},{"category":"function","location":"api/#GNSSReceiver.band_ids","page":"API Reference","text":"band_ids(plan) -> Vector{Symbol}\n\nEvery band the plan routes, in order, the reference band first.\n\n\n\n\n\n","title":"GNSSReceiver.band_ids"},{"category":"function","location":"api/#GNSSReceiver.band_route","page":"API Reference","text":"band_route(plan, band_id) -> Union{Nothing,HardwareBandRoute}\n\nThe route for band_id, or nothing when the plan does not carry that band.\n\n\n\n\n\n","title":"GNSSReceiver.band_route"},{"category":"function","location":"api/#GNSSReceiver.band_sampling_frequency","page":"API Reference","text":"band_sampling_frequency(plan, band_id) -> Float64\n\nThe rate in Hz that band_id's samples, dump sample_indexes and replica tap offsets are counted at.\n\n\n\n\n\n","title":"GNSSReceiver.band_sampling_frequency"},{"category":"function","location":"api/#GNSSReceiver.receiver_timebase_scale","page":"API Reference","text":"receiver_timebase_scale(plan, band_id) -> Float64\n\nHow many receiver-timebase samples one of band_id's samples is worth: reference_sampling_frequency(plan) / band_sampling_frequency(plan, band_id). Exactly 1.0 for the reference band, and for every band of a device that samples all of them at one rate — which is why a single-band receiver's arithmetic is untouched by any of this.\n\n\n\n\n\n","title":"GNSSReceiver.receiver_timebase_scale"},{"category":"function","location":"api/#GNSSReceiver.to_receiver_samples","page":"API Reference","text":"to_receiver_samples(plan, band_id, sample) -> Int64\n\nMap a count on band_id's device counter onto the receiver timebase.\n\n\n\n\n\n","title":"GNSSReceiver.to_receiver_samples"},{"category":"function","location":"api/#GNSSReceiver.to_band_samples","page":"API Reference","text":"to_band_samples(plan, band_id, sample) -> Int64\n\nMap a count on the receiver timebase onto band_id's own device counter — the inverse of to_receiver_samples, and what a handover time or an an NCO word's landing sample is expressed in before it is handed to the device.\n\n\n\n\n\n","title":"GNSSReceiver.to_band_samples"},{"category":"function","location":"api/#GNSSReceiver.band_plan_error","page":"API Reference","text":"band_plan_error(capabilities, plan) -> Union{Nothing,String}\n\nEvery reason a device with capabilities cannot receive plan's bands at the same time, as one message — or nothing when it can.\n\nThis is the RF-capacity gate, and it is deliberately separate from the per-signal one: a device may be perfectly able to replicate and correlate every signal it is asked for and still have exactly one tuner. Supporting every signal individually is not the same as receiving every band simultaneously, and the receiver never quietly configures a subset.\n\nThe checks:\n\nevery band is one the front end declares it can tune;\nno more bands than the device has RF inputs — per device, since that is what the declaration counts;\nno two bands on one RF input of one device;\na multi-device plan has a declared clock relationship that makes its counters comparable (see HardwareBandPlan).\n\nThere is no automatic fallback to sequential retuning — receiving band A for a while, then retuning to band B. It would silently turn a simultaneous request into a time-multiplexed one, which changes the C/N₀, the measurement epochs and the fix rate of every band involved, and leaves no band continuously tracked. A receiver that wants it runs one receive per band configuration and retunes between them, which is explicit and is what the error below says.\n\n\n\n\n\n","title":"GNSSReceiver.band_plan_error"},{"category":"function","location":"api/#GNSSReceiver.band_bank_error","page":"API Reference","text":"band_bank_error(sdr, plan) -> Union{Nothing,String}\n\nEvery reason sdr's correlator banks cannot serve plan's bands, as one message — or nothing when they can.\n\nA band whose bank (band_hardware_channels) holds no channel in range is a band the receiver can tune and then never track anything on: its satellites are acquired, find no channel of their own bank, and wait unassigned while their lock detectors quietly release them. Two bands sharing a channel is the same failure wearing the other hat — whichever band claims it first leaves the other one short, and which one that is depends on acquisition order.\n\nBoth are exactly the silent partial configuration this gate exists to prevent, so they are refused before anything is armed rather than discovered as a band that never locks.\n\n\n\n\n\n","title":"GNSSReceiver.band_bank_error"},{"category":"function","location":"api/#GNSSReceiver.get_gui_data_channel","page":"API Reference","text":"get_gui_data_channel(data_channel, push_gui_data_roughly_every = 500ms)\n\nReturn a Channel{GUIData} that downsamples data_channel for display: a spawned task consumes every ReceiverDataOfInterest but only forwards one roughly every push_gui_data_roughly_every of signal runtime (plus the very first), so the GUI is refreshed at a human rate rather than once per processed chunk.\n\nThe forwarding task also propagates a downstream close upstream: if the consumer closes the returned channel — which gui does when the user quits — it closes data_channel, ending the run instead of leaving the pipeline churning through the rest of the stream with nobody watching.\n\n\n\n\n\n","title":"GNSSReceiver.get_gui_data_channel"},{"category":"function","location":"api/#GNSSReceiver.default_data_of_interest","page":"API Reference","text":"default_data_of_interest(receiver_state) -> ReceiverDataOfInterest\n\nCondense a ReceiverState into the default per-chunk payload emitted by receive: each tracked satellite's CN0, prompt correlator value and health — keyed by (signal_id, prn) — together with the current PVT solution, the runtime and the vector-tracking status.\n\nThis is the default extract function of receive. Pass your own extract(receiver_state) to emit a different payload (e.g. raw carrier Doppler, code phase or decoded navigation data); the returned channel's element type is inferred from what it returns. extract runs inside the tracking loop on a ReceiverState that the next chunk mutates in place, so it must be read-only and return an immutable value.\n\n\n\n\n\n","title":"GNSSReceiver.default_data_of_interest"},{"category":"function","location":"api/#GNSSReceiver.save_data","page":"API Reference","text":"save_data(data_channel; filename) -> Task\n\nConsume every element of data_channel on a spawned task and, once the channel closes, write the collected vector to filename as a JLD2 file under the key \"data_over_time\".\n\nThis is the on-disk counterpart to collect_data: like it, save_data works with the default ReceiverDataOfInterest channel from receive as well as a channel of a custom payload produced via receive's extract keyword. Returns the writer task immediately (the write happens when the producer finishes); wait on it if you need to block until the file is on disk.\n\n\n\n\n\n","title":"GNSSReceiver.save_data"},{"category":"function","location":"api/#GNSSReceiver.collect_data","page":"API Reference","text":"collect_data(data_channel) -> Vector\n\nConsume every element of data_channel, blocking until the channel closes, and return them in arrival order as a Vector of the channel's element type.\n\nThis is the in-memory counterpart to save_data: use it to gather a whole run for offline analysis or plotting (e.g. last(collect_data(data_channel)) for the final snapshot), and use save_data to persist directly to a JLD2 file instead. It works with the default ReceiverDataOfInterest channel from receive as well as a channel of a custom payload produced via receive's extract keyword.\n\n\n\n\n\n","title":"GNSSReceiver.collect_data"},{"category":"function","location":"api/#GNSSReceiver.Dashboard.gui","page":"API Reference","text":"gui(gui_data_channel; fps = 12)\n\nDisplay the receiver dashboard, consuming each GUIData from gui_data_channel. Runs a Tachikoma terminal app: a background task keeps the model fed with the latest frame while the app renders the CN0 bars, the direction-of-arrival sky plot, the Position/Velocity/Time block and an OpenStreetMap map of the fix. When the stream ends the last frame stays on screen (flagged \"stream ended\"); the app blocks until the user quits (q or Ctrl-C). Keys: d toggles the PVT diagnostics; +/- zoom and hjkl pan the map, 0 recenters it.\n\nQuitting closes gui_data_channel, which tears the pipeline down behind it (see get_gui_data_channel) — q stops the run, it does not just hide it.\n\n\n\n\n\n","title":"GNSSReceiver.Dashboard.gui"},{"category":"type","location":"api/#GNSSReceiver.ReceiverDataOfInterest","page":"API Reference","text":"ReceiverDataOfInterest\n\nSnapshot of the receiver after a processed chunk: sat_data maps each tracked satellite to its SatelliteDataOfInterest, pvt is the current PVT solution, runtime is the elapsed signal time and vt is the VTStatus (or nothing when vector tracking is disabled). This is the element type produced by receive.\n\nsat_data is a Dictionaries.Dictionary (GNSSReceiver.jl PR #96) keyed by (signal_id, prn) — the ranging signal's id and PRN — so the same PRN can appear on several constellations and bands without colliding, and the key matches pvt.sats (which PositionVelocityTime keys by the same ranging signal).\n\n\n\n\n\n","title":"GNSSReceiver.ReceiverDataOfInterest"},{"category":"type","location":"api/#GNSSReceiver.SatelliteDataOfInterest","page":"API Reference","text":"SatelliteDataOfInterest\n\nPer-satellite summary emitted for each processed chunk: the estimated carrier-to-noise density ratio cn0, the latest fully integrated prompt correlator value (a scalar for single-antenna, an SVector for multi-antenna), whether the satellite reports itself is_healthy, whether its tracking loops are currently closed by the vector-tracking navigation filter (in_vt_loop; always false when VT is disabled), whether those loops have settled enough to range on (is_ranging_ready) and whether the lock detectors currently declare it locked (is_in_lock).\n\nLoop membership is not the same as having determined the solution: a member coasting through an obscuration stays in_vt_loop and keeps being steered by the filter, but withholds its discriminators. Which satellites were measured is pvt.sats, under vector and scalar tracking alike — see VTStatus for the coasted ones.\n\nis_healthy and is_ranging_ready are independent, and neither implies the other: is_healthy is what the satellite says about itself in its navigation message, while is_ranging_ready is what the receiver says about its own loops. Both must hold for the satellite to contribute to the PVT solution.\n\nWhat is_ranging_ready distinguishes is the stage within lock: the acquisition → tracking handover is deliberately tolerated (the point is to keep a converging satellite rather than drop and reacquire it), so a satellite can be tracked and reported here for some time before its code phase is trustworthy enough to range on — from 680 code periods for a healthy signal up to 2000 for one whose evidence never comes cleanly. Without this field such a satellite is indistinguishable from one the PVT solve is ignoring for no reason.\n\nis_in_lock is the other half of that picture, and it is not constant-true here: a satellite that loses lock is normally dropped from tracking before the next payload is built (remove_lost_satellites), but a vector-loop member never is — the navigation filter keeps steering it through an outage and decides itself when to release it. Such a member reports in_vt_loop && !is_in_lock, and is_ranging_ready stays latched from before the outage (deliberately: the filter kept both its NCOs steered, so it is rangeable the moment the signal returns). So is_ranging_ready alone does not answer \"would the receiver range on this satellite right now?\" — collect_pvt_sat_states! requires both, and a consumer that colours or filters on readiness wants both too. See is_ranging_ready, is_in_lock and LockDwell.\n\n\n\n\n\n","title":"GNSSReceiver.SatelliteDataOfInterest"},{"category":"type","location":"api/#GNSSReceiver.AbstractLockDetector","page":"API Reference","text":"AbstractLockDetector\n\nSupertype for the receiver's per-satellite lock detectors. A concrete detector is advanced with update and queried with is_in_lock; a satellite is treated as locked only while both its code and carrier detectors report lock.\n\nThe detectors track elapsed signal time rather than a number of update calls, so their behaviour is independent of how the incoming signal is chunked: each update is handed the signal_duration it represents and accumulates that duration into its timers.\n\nWhy the timings are in primary code periods, not seconds\n\nTracking sizes its default loop bandwidths from the primary code period T, at B_L·T ≈ 0.018: B_L = 0.018/T for the carrier loop and B_L/18 for the code loop. So every loop time constant scales with T — 1/B_L is 55.6·T for the carrier and 1000·T for the code loop:\n\nsignal T carrier B_L code B_L 1/B_L carrier 1/B_L code\nGPS L1 C/A 1 ms 18 Hz 1 Hz 56 ms 1.0 s\nGalileo E1B 4 ms 4.5 Hz 0.25 Hz 222 ms 4.0 s\nGPS L1C-D 10 ms 1.8 Hz 0.1 Hz 556 ms 10.0 s\n\nA dwell fixed in seconds therefore means something different on every signal. The 200 ms this receiver used to allow is 200 code periods on GPS L1 C/A — enough — but only 50 on Galileo E1B and 20 on GPS L1C-D, against loops that are 4× and 10× slower. That is why lock detection failed on the slower signals while GPS L1 C/A looked fine. Counted in code periods the same number means the same thing everywhere, and the GPS L1 C/A defaults are reproduced exactly (T = 1 ms, so 200 code periods is 200 ms).\n\nSo every detector takes a reference_integration_time — one primary code period of the signal it runs on, which the receiver passes from primary_code_period — and takes its timings as counts of that period. The fields themselves stay in seconds.\n\n\n\n\n\n","title":"GNSSReceiver.AbstractLockDetector"},{"category":"type","location":"api/#GNSSReceiver.LockDwell","page":"API Reference","text":"LockDwell\n\nThe out-of-lock dwell shared by the receiver's lock detectors: a timer that accumulates while the detector's statistic reports \"no usable signal\" and declares lock lost once it reaches a threshold. Both CodeLockDetector and CarrierLockDetector hold one, so the handover staging below is defined in exactly one place. Its timings are counts of one primary code period — see AbstractLockDetector for why.\n\nWhy there are two stages\n\nThe acquisition → tracking handover is deliberately coarse: the acquisition Doppler bin is sized to half the loop's pull-in range (receive's pull_in_margin), and the code phase is only resolved to the sample grid, halved by subsample_interpolation. The loops need time to pull those errors in, and until they have, the prompt correlator sits off the correlation peak and carries less power than the satellite really has — so a healthy satellite reads below its true C/N0, and its carrier phase is still spinning at the residual Doppler, so the phase-lock indicator reads near zero.\n\nMeasured on synthetic handovers at this receiver's own worst-case residual (see the commit that introduced this type), the peak accumulated out-of-lock time a satellite the receiver should keep demands is 292 code periods — GPS L1 C/A at 32 dBHz, a 0.25-chip and 125 Hz handover at 2.048 MHz. That is more than the 200-code-period steady-state dwell tolerates, so a single un-staged dwell would drop it. The carrier detector's demand is larger again, 460 to 590 code periods at a healthy 34 dBHz, because the FLL has to pull in the residual Doppler before cos(2φ) stops averaging to zero.\n\nSo the dwell has two stages:\n\npull-in, from construction: tolerates pull_in_out_of_lock_time_threshold, sized to survive the whole handover transient with margin over the measured worst case;\nsteady state, latched permanently once the handover is over: tolerates the tighter out_of_lock_time_threshold.\n\nTwo stages rather than one long dwell, because they answer different questions. A single dwell would have to be ≳600 code periods to be safe, and would then take three times as long to notice a satellite that genuinely disappeared. Staging buys handover tolerance without paying for it in steady-state detection latency.\n\nThe handover counts as over on either of two conditions:\n\nconfirm_time_threshold of uninterrupted favourable evidence with nothing left on the out-of-lock clock — the satellite has demonstrably converged, so there is no reason to keep extending it handover credit;\npull_in_time_threshold has elapsed and the accumulated out-of-lock time is under the steady-state threshold — the backstop for a satellite that never quite settles.\n\nBoth are needed. Without the evidence-based one, a satellite that converged immediately would still be treated as pulling in until the timer expired, and a fade during that window would take the pull-in dwell to notice. Without the timer, a satellite that never strings together a clean run never leaves the stage.\n\nRequiring the accumulated time to be under the steady-state threshold before latching matters too: otherwise a satellite sitting comfortably inside the generous pull-in threshold would be declared lost the instant the tighter one took over.\n\nOne residual case is left open deliberately. Both latches require the out-of-lock clock to be under the steady-state threshold, so a satellite whose clock parks between the two allowances — 200 to 600 code periods — never leaves the pull-in stage, and so never becomes ranging-ready either. It takes a burst of failures followed by a sustained near-50% duty cycle; stochastically the clock random-walks out of that band, either down to zero (whereupon it latches) or past the pull-in allowance (whereupon lock is lost and the satellite is reacquired). Closing it would mean letting the pull-in stage latch with its clock still above the steady-state threshold, which is exactly the discontinuity the latch guard prevents.\n\nWhy ranging readiness is a separate, later latch\n\npulled_in answers \"may the dwell tighten?\" and wants to fire early, so a fade after a successful handover is caught on the steady-state timescale. A pseudorange consumer needs something different and slower: the code loop to have settled. Those two conflict, and measurement shows one threshold cannot serve both.\n\nC/N0 recovers well before the code phase does — on BPSK the prompt only loses 1 − |Δτ|, so a GPS L1 C/A satellite reads healthy almost immediately while its DLL (1/B_L = 1000 code periods) is still walking the handover error in. Measured at the same 0.25-chip handover, the residual code phase is 0.15 to 0.22 chip after 200 code periods — 44 to 64 m of pseudorange error — falling to 0.018 to 0.15 chip, 5 to 44 m, by 600. So there are two latches over the same settled_time: confirm_time_threshold (200 code periods) tightens the dwell, and ranging_confirm_time_threshold (600) admits the satellite to ranging via is_ranging_ready. It is floored at confirm_code_periods, since ranging can never be ready before the handover is over.\n\nBeing conservative on the second is close to free: the earliest it can fire is the warm-up plus one clean run, 680 code periods, and measured on real handovers it lands at 760 to 1500 on GPS L1 C/A — the carrier loop has its own residual Doppler to pull in before the phase-lock indicator stops resetting the run — so still inside receive's own 2 s time_in_lock_before_calculating_pvt gate. On slower signals the extra seconds are dwarfed by the tens of seconds of subframe decoding that actually set time to first fix.\n\nLike the pull-in stage, ranging readiness needs a timer as well as an evidence path, and for the same reason: settled_time is reset by any out-of-lock verdict, so an uninterrupted run of 600 is a bar a satellite can fail to clear indefinitely — while sitting comfortably in lock, tracked, converged, and silently absent from the PVT solve. Since PVT needs four satellites, that trades a degraded fix for no fix.\n\nUnder Tracking's default noise-reference CN0 estimator the code detector rarely blocks that run: measured steady-state per-chunk out-of-lock rates with no handover error are 0.58 at the 30 dBHz threshold itself, 0.10 one dB above it and 0.00 from two dB up — so a satellite the code detector holds at all completes a clean run easily. The carrier detector is what blocks it: its 460-to-590-code-period pull-in transient resets settled_time well after the code detector is happy, and any recurring disturbance shorter than the run reopens the same hole at any C/N0. So ranging_pull_in_time_threshold (2000 code periods) backstops it. It also takes pulled_in as a conjunct, so \"ranging is never ready before the handover is over\" holds structurally rather than by threshold ordering.\n\nWhat the backstop relaxes is the length of the clean run, not the requirement that the channel be clean at all: it fires only with the out-of-lock clock paid down to zero. A weaker guard — \"the clock is within what steady state tolerates\" — reads like the one pulled_in's timer uses, but the two are not the same test. pulled_in's guard exists to stop the threshold switch itself from declaring loss, so it is only ever consulted where the pull-in allowance exceeds the steady-state one. A health guard has to be reachable instead, and the tolerance form is not: the clock can never exceed elapsed - warm_up_time_threshold, so wherever the dwell outlasts the backstop it cannot fail before the timer fires. The carrier detector is exactly that case — a 4000-code-period dwell against a 2000-period backstop — and under the tolerance form a channel whose phase-lock indicator had never once passed would be admitted to ranging at 2000 code periods, then held there by resets_on_lock on one favourable chunk in a thousand. Requiring zero is reachable for both detectors (a single favourable chunk clears it under resets_on_lock, a repaid debt otherwise), cannot be made vacuous by any configuration, and keeps the deterministic case the backstop exists for: a disturbance recurring faster than the clean run still leaves the clock at zero between disturbances, whereas a channel failing steadily enough never to pay its debt off is dropped by is_in_lock rather than admitted here.\n\nTwo code-loop time constants (1/B_L = 1000 code periods) rather than anything shorter, because the backstop admits a measurement it has not seen clean evidence for, and what the DLL's convergence depends on is elapsed time, not clean-run length. What it concedes is thermal jitter on a marginal satellite's code phase, not a handover bias: for the default 1 Hz code loop and Tracking's 1-chip early-late spacing, Kaplan & Hegarty's coherent-DLL expression gives 1σ = 0.039 chip (11 m) at 30 dBHz and 0.027 chip (8 m) at 32, against 0.004 chip (1.2 m) at 45 — inside the same band the evidence path already accepts. On GPS L1 C/A the timer is 2 s, exactly receive's default time_in_lock_before_calculating_pvt, so there it costs nothing.\n\nBefore warm_up_time_threshold no evidence is accumulated at all. Under the noise-reference estimator an unmeasured channel reads -Inf dBHz and so fails the CN0 test rather than passing it, so the warm-up is not there to hide an estimator's cold bias — it is there for the carrier detector's own moving averages, which start empty (see CarrierLockDetector).\n\nresets_on_lock selects how favourable evidence is credited. false pays the accumulated time back one signal_duration at a time. true clears it outright, which is the \"optimistic\" detector of Kaplan & Hegarty §5.11.2 — it \"decides quickly and changes its mind slowly\", so declaring loss needs a consecutive run of failures. The carrier detector uses it, because a marginal satellite's phase-lock indicator dips below threshold intermittently and a paying-back accumulator would random-walk across any threshold.\n\n\n\n\n\n","title":"GNSSReceiver.LockDwell"},{"category":"type","location":"api/#GNSSReceiver.CodeLockDetector","page":"API Reference","text":"CodeLockDetector <: AbstractLockDetector\n\nDeclares code lock from the estimated carrier-to-noise density ratio. After a warm-up it accumulates out-of-lock time whenever the CN0 is below cn0_threshold (and pays it back down otherwise); lock is lost once the accumulated out-of-lock time reaches the threshold its LockDwell stage allows — generous while the loops pull the acquisition handover in, tighter in steady state. Every timing is a count of reference_integration_time; the dwell's keywords are accepted here and forwarded to it.\n\nThe accumulated time is capped at a small multiple of what the current stage tolerates. Since it is paid back at the rate it is spent, an uncapped dwell would make re-declaring lock take as long as the outage that preceded it — minutes, for a satellite kept in tracking through a tunnel by the vector-tracking loop. With the cap, one stage threshold of good signal always brings a satellite back.\n\nThe comparison is made on the post-integration SNR, CN0 · T, rather than on CN0 alone. T is the integration time of the record the CN0 estimate came from (Tracking's get_last_fully_integrated_integration_time), and cn0_threshold is the CN0 demanded over reference_integration_time — one primary code period. A record spanning N code blocks therefore clears the detector at 10·log10(N) dB less CN0, which is what the longer coherent integration genuinely buys: detectability is set by the post-integration SNR, not by CN0 on its own.\n\nAnchoring to the primary code period rather than to the symbol period keeps cn0_threshold meaning exactly what it meant for one-block records (the default tracking cadence), and needs no data rate — so it is also defined for pilot signals, where get_data_frequency is 0 Hz.\n\ncoherence_limit caps the credited T, and defaults to Inf·s — uncapped — because Tracking already bounds a record at the coherent limit itself: post-sync it integrates clamp(preferred_num_code_blocks_to_integrate, 1, blocks_per_symbol) blocks, where a symbol is the data-bit period, or the secondary-code period for a pilot. That bound is generous for a long overlay (GPS L1C-P's is 1800 blocks), so set coherence_limit when crediting a full symbol is more than the loop should be trusted with.\n\nNote that Tracking's preferred_num_code_blocks_to_integrate defaults to 1 and this receiver never raises it, so every record is currently one code block and the credit is exactly cn0_threshold — the N > 1 behaviour above only engages once that is plumbed through.\n\nThe estimate itself comes from Tracking's default CN0 estimator, which as of Tracking 7 is the measured noise reference (NoiseRefCN0Estimator): each record's prompt power is divided by a noise density Tracking measures by despreading the signal's own code at a randomised phase, through the same correlator the satellites go through. cn0_threshold is unchanged in units and meaning, but the number it is compared against is a different — and better behaved — statistic:\n\nit has no noise-only floor to clear. The moment ratio manufactured signal power out of noise, reporting a median of ~27.6 dB-Hz on pure noise, so a threshold below ~30 dB-Hz could never trip and one at 30 dB-Hz was cleared by noise on ~19 % of records. The noise reference's per-record terms average to about zero there — -Inf dB-Hz on about half the realizations and single digits on the rest — so the out-of-lock dwell is spent monotonically: a satellite that dies drops after exactly out_of_lock_time_threshold, rather than the ~1.3× the detector used to take paying time back on the noise realizations that cleared the threshold;\nit reads the true CN0 rather than a biased one, because the floor is measured instead of inferred from the same prompts. In the synthetic model the tests drive (a perfectly phase-locked prompt in unit-power noise) it lands within 0.1 dB of the truth from 25 dB-Hz up, where the moment ratio reads 2.9 dB high at 25 dB-Hz. On the ION reference recording it reads a median 1.0 dB above what the narrowband/wideband ratio (Tracking 6's default) reported, and up to 2.4 dB on the least steadily tracked satellite: that estimator summed ~5 ms of prompts coherently and charged the loop's residual phase noise to the signal, and this one does not;\nT is applied per record, as the record is folded, rather than when the estimate is read. That changes nothing for the detector — it multiplies the record's own T back in either way — but it does mean estimate_cn0 reports the same true CN0 whatever the record length, instead of a number that has to be read together with one.\n\nAn unmeasured channel — a TrackState with no noise source for the signal, or one whose window is still empty — reports -Inf dB-Hz, which needs no special handling in the threshold test (it linearizes to 0 Hz). The receiver provisions a noise source for every signal it tracks (GNSSReceiver.create_noise_estimators), so that is the transient before the first measurement rather than a configuration a user can land in. At four or more antennas the same -Inf can extend over the first chunk or two, while the array's spatial noise covariance is still rank-deficient in its own dimensions — and only for a receiver fed buffers of about one code period, since a longer chunk clears it within its first call. Either way the detector's own warm-up (warm_up_code_periods, 80 code periods) is orders of magnitude longer — on every signal, since it scales with the code period too — so it never accumulates against it.\n\n\n\n\n\n","title":"GNSSReceiver.CodeLockDetector"},{"category":"type","location":"api/#GNSSReceiver.CarrierLockDetector","page":"API Reference","text":"CarrierLockDetector <: AbstractLockDetector\n\nDeclares carrier lock from the prompt correlator using the Van Dierendonck narrowband-difference / narrowband-power phase-lock indicator\n\nPLI = Σ(Iₖ² − Qₖ²) / Σ(Iₖ² + Qₖ²)\n\naccumulated over every prompt the tracking loops produce. For a prompt of amplitude A in noise of total variance σ² — post-integration SNR ρ = A²/σ² = CN0·T — at carrier phase error φ,\n\nE[Iₖ² − Qₖ²] = A²·cos(2φ)      E[Iₖ² + Qₖ²] = A² + σ²\n\nso E[PLI] = cos(2φ)·ρ/(ρ+1). Lock is declared while PLI ≥ phase_lock_threshold; its LockDwell clears outright on any favourable chunk (resets_on_lock), so declaring loss needs a consecutive run of failures. Its default out_of_lock_code_periods of 4000 is 4 s on GPS L1 C/A, matching both this receiver's historical value and Ward's L0 = 240 × 20 ms = 4.8 s. Because pull_in_out_of_lock_code_periods is floored at that, both stages are equal and the carrier detector is deliberately unstaged — its dwell already asks for a consecutive run of failures far longer than any handover transient.\n\nThis replaces a low-pass-filtered |I|-versus-|Q| amplitude comparison (lowpass(|I|)/K2 > lowpass(|Q|) with K1 = 0.0247, K2 = 1.5 — the detector of Kaplan & Hegarty §5.11.2). Three concrete reasons:\n\nIt sees every prompt. The old detector was fed one prompt per processing chunk, discarding three of every four at the receiver's 4 ms chunk over a 1 ms code period.\nIts averaging is continuous. The old filter state was reset every 80 ms block, so the value actually tested had only reached ~39% of its asymptote — K1 = 0.0247 is specified for 20 ms epochs and gives a 40-epoch time constant, which the 20-update block truncated.\nIts expectation is known in closed form, so a threshold maps to an explicit phase error at a given C/N0. The old comparison only approaches a phase test at high SNR; at low SNR it degenerates into an implicit SNR test (at perfect phase lock it flips at ρ ≈ 0.54, i.e. ~27.4 dBHz over a 1 ms record), and that crossover is a consequence of K2 rather than something you can read off it.\n\nphase_lock_threshold defaults to 0.25, matching PocketSDR's THRES_PLI. Because the indicator saturates at ρ/(ρ+1), a threshold has to be read together with the C/N0 one: at the default 30 dBHz over a 1 ms record ρ = 1, so even perfect phase lock reads only 0.5, and 0.25 demands cos(2φ) ≥ 0.5 — |φ| ≤ 30°, comparable to the 33.7° K2 = 1.5 implied. A threshold above 0.5 would be unreachable at the C/N0 the code detector still accepts. Read as a pure sensitivity floor the default is ρ ≥ 1/3, i.e. 25.2 dBHz at a 1 ms record, so — as before — the code detector's 30 dBHz stays the binding limit and the carrier detector is left to judge phase.\n\nThe two sums are tracked as exponential moving averages sharing one gain, so the indicator is continuous. smoothing_records sets the averaging window; the default of 200 keeps the noise-only indicator's median at 0.00 and its 99th percentile at 0.12 — both well clear of the 0.25 threshold — while a 36 dBHz signal reads 0.78 against its 0.80 asymptote.\n\nThe window is counted in records, not code periods, unlike the two timings: the gain is applied once per prompt, and a prompt is one record spanning get_last_fully_integrated_num_code_blocks code blocks. The timings stay honest in code periods because they are driven by signal_duration, which is real elapsed time; this average has no access to that. The two coincide today only because Tracking's preferred_num_code_blocks_to_integrate is 1 and this receiver never raises it (see CodeLockDetector) — once that is plumbed through, a 200-record window will be 200·N code periods long.\n\nThose noise-only figures are asymptotic, and the 80-code-period warm-up is shorter than the 200-record window, so the first judgement is made on an average that has not converged. Measured over 4000 noise realizations at the default window, the p99 settles at 0.12 but is 0.17 at 200 records, 0.26 at 80 (max 0.38) and 0.35 at 40 — i.e. at the moment the warm-up ends a noise-only channel is above threshold 1.2% of the time. The warm-up is deliberately not lengthened to match: clearing the dwell on any favourable chunk, together with the 4000-code-period dwell, means declaring loss needs a consecutive run of failures, so an early excursion in either direction is absorbed. In the other direction the early window is already tight enough: at the code-lock threshold (ρ = 1, perfect phase lock) the indicator is below 0.25 for 0.1% of realizations at 80 records and none at 200.\n\n\n\n\n\n","title":"GNSSReceiver.CarrierLockDetector"},{"category":"function","location":"api/#GNSSReceiver.is_in_lock","page":"API Reference","text":"is_in_lock(lock_detector::AbstractLockDetector)\n\nReturn true while the detector's accumulated out-of-lock time is below the threshold its current stage allows (see LockDwell).\n\n\n\n\n\n","title":"GNSSReceiver.is_in_lock"},{"category":"function","location":"api/#GNSSReceiver.has_pulled_in","page":"API Reference","text":"has_pulled_in(lock_detector::AbstractLockDetector)\n\nWhether the detector has left its pull-in stage — the acquisition → tracking handover is over and the satellite is being held to steady-state standards (see LockDwell).\n\nThis is about the dwell, not about measurement quality: it fires as soon as the C/N0 deficit of the handover transient has cleared, which on a BPSK signal happens well before the code loop has settled. Use is_ranging_ready to decide whether to range on the satellite.\n\nThe receiver itself gates nothing on this; it is exposed for introspection and diagnostics — \"which stage is this channel in?\" — while is_ranging_ready is what actually gates ranging (see collect_pvt_sat_states!) and is_in_lock what gates tracking.\n\n\n\n\n\n","title":"GNSSReceiver.has_pulled_in"},{"category":"function","location":"api/#GNSSReceiver.is_ranging_ready","page":"API Reference","text":"is_ranging_ready(lock_detector::AbstractLockDetector)\n\nWhether the tracking loops have settled enough for this satellite's measurements to be trustworthy — the question a pseudorange consumer has to ask.\n\nis_in_lock answers \"should this satellite still be tracked?\" and is deliberately tolerant through the handover, because the point is to keep a converging satellite rather than drop and reacquire it. But while the loops are still walking the coarse acquisition estimate in, the code phase — and so the pseudorange — carries a converging bias, measured at tens of metres. This latches after ranging_confirm_time_threshold of uninterrupted good evidence or, for a satellite whose evidence is never uninterrupted, once ranging_pull_in_time_threshold of signal has elapsed with nothing left on the out-of-lock clock — the backstop shortens the clean run it asks for, it does not waive it. It never un-latches: a satellite whose loops settled once has settled, and later disturbances are is_in_lock's business — which also means a vector-tracking member carried through an outage is immediately rangeable again on return, as it should be, since the navigation filter kept both its NCOs steered throughout.\n\nThe timer is what keeps a marginal-but-tracked satellite from being excluded from PVT forever; see LockDwell for the measurements behind both thresholds, and for what the timer concedes in exchange (a few metres of extra code-phase jitter, not a handover bias).\n\n\n\n\n\n","title":"GNSSReceiver.is_ranging_ready"},{"category":"function","location":"api/#GNSSReceiver.phase_lock_indicator","page":"API Reference","text":"phase_lock_indicator(lock_detector::CarrierLockDetector)\n\nThe smoothed phase-lock indicator, or NaN before any prompt has been folded in.\n\n\n\n\n\n","title":"GNSSReceiver.phase_lock_indicator"},{"category":"type","location":"api/#GNSSReceiver.EigenBeamformer","page":"API Reference","text":"EigenBeamformer{N} <: AbstractPostCorrFilter\n\nPost-correlation filter for an N-antenna array that steers towards a satellite by eigen-beamforming. It accumulates the prompt correlator's spatial covariance and, every calc_new_every updates, recomputes the beamforming weights from the dominant eigenvector (the estimated signal subspace) before resetting the accumulator.\n\nConstruct one with EigenBeamformer(num_ants); Tracking reads its current weights through Tracking.get_weights and combines the correlator's per-antenna taps itself, and Tracking.update evolves it.\n\n\n\n\n\n","title":"GNSSReceiver.EigenBeamformer"},{"category":"function","location":"api/#GNSSReceiver.process","page":"API Reference","text":"process(receiver_state, acq_plans, measurements, band_systems, sampling_freq,\n        interm_freqs = map(_ -> 0.0Hz, band_systems); kwargs...)\n\nAdvance the receiver by one measurement chunk per band and return the next ReceiverState.\n\nA single chunk runs the whole per-cycle pipeline: it (re)acquires satellites per band via acq_plans at most every acquire_every (buffering samples only when acquisition could fire), tracks every band's satellites from one TrackState, updates their lock detectors and, once enough have been locked for time_in_lock_before_calculating_pvt, recomputes the fused multi-GNSS PVT solution every pvt_update_interval. Satellites that drop out of lock are removed and picked up again by the periodic scan (the fast reacquisition path of should_reacquire is off by default — see there). measurements, band_systems and interm_freqs are tuples aligned band-by-band (interm_freqs defaults to 0 Hz for every band), and acq_plans is one NamedTuple keyed by group key across all bands. sampling_freq is one frequency for every band, or a tuple aligned with them for a front end whose bands run at different rates; the bands' frames must then still span the same duration, and the first band is the receiver's clock. This is the function receive calls for each chunk; see it for the remaining keyword arguments.\n\n\n\n\n\n","title":"GNSSReceiver.process"},{"category":"type","location":"api/#GNSSReceiver.VectorTracking","page":"API Reference","text":"VectorTracking(; use_pseudorange_rates = true,\n               motion_model_order = 2, clock_model_order = 2,\n               acceleration_noise_std = 5.0m/s^2,\n               h0 = 2e-19, hm2 = 2e-20,\n               ifb_noise_density = 0.01m/sqrt(1.0s),\n               insufficient_meas_timeout = 10.0s)\n\nConfiguration of the vector-tracking navigation filter. Pass an instance as the vector_tracking keyword of receive / ReceiverState to describe the platform and the receiver's oscillator; vector_tracking = true uses these defaults, which suit a vehicle carrying a consumer-grade front end.\n\nFields\n\nuse_pseudorange_rates: with true (VDFLL) the navigation filter measures both the pseudoranges (DLL discriminators) and the pseudorange rates (FLL discriminators on the carrier Doppler); with false (VDLL) only the pseudoranges enter the filter — the carrier NCO corrections are then derived purely from the filter's velocity/clock-drift prediction.\nmotion_model_order: order of the per-axis motion model — 1 position only, 2 position + velocity, 3 position + velocity + acceleration.\nclock_model_order: order of the receiver clock model — 1 biases only, 2 biases + a common drift. One clock bias is modelled per GNSS time system, all integrating the single oscillator drift.\nacceleration_noise_std: white-acceleration process-noise standard deviation driving the motion model — how hard the platform manoeuvres. The 5 m/s² default suits automotive / vehicular dynamics (≈0.5 g of manoeuvring); use ~1 m/s² for pedestrian or ship dynamics and tens of m/s² for aircraft or launch vehicles. It means the same thing at every motion_model_order: the process noise always models the platform's first unmodelled derivative, so the orders that do not model the acceleration (1 and 3) derive their velocity and jerk figures from this one through the manoeuvre time constant MANOEUVRE_TIME — see motion_noise_model, which is also where to change that constant for a platform whose manoeuvres are much shorter or longer than the couple of seconds a road vehicle takes.\nh0, hm2: Allan-variance coefficients h_0 (seconds) and h_-2 (1/seconds) of the receiver oscillator, sizing the clock process noise — smaller coefficients for a more stable oscillator. They follow from the oscillator's Allan deviation: σ_y²(τ) = h_0  2τ at short averaging times gives h_0 = 2τσ_y²(τ), and σ_y²(τ) = (2π²3)h_-2τ at long ones gives h_-2 = 3σ_y²(τ)(2π²τ). The defaults model a TCXO-grade clock (a typical consumer/automotive receiver oscillator); an OCXO is orders of magnitude tighter, a bare crystal looser.\nifb_noise_density: random-walk process-noise density of the inter-frequency biases, in m/√s. Per-band RF-chain delays are nearly constant, so this only has to cover thermal drift of the front end: the default lets a bias wander about 0.01 m in a second of elapsed time. Raise it for a front end whose bands are less thermally coupled, lower it for one that is stable or externally calibrated.\ninsufficient_meas_timeout: how long the navigation filter may coast on epochs it cannot solve before vector tracking is abandoned and the receiver falls back to scalar tracking. An epoch counts as unsolvable when it has fewer measurements than the bias layout in force has unknowns. How certain the filter is of the solution it did produce is reported rather than policed — see VTStatus's position_std.\n\n\n\n\n\n","title":"GNSSReceiver.VectorTracking"},{"category":"type","location":"api/#GNSSReceiver.VectorTrackingState","page":"API Reference","text":"VectorTrackingState\n\nRuntime state of the vector-tracking loop, carried on the ReceiverState: the VectorTracking configuration, the clock/inter-frequency-bias layout derived from the configured systems, the navigation filter's process model and Kalman state/covariance, whether the vector loop is running (it starts running with the first scalar PVT fix and falls back on prolonged measurement starvation), which clock-bias state is the reporting reference (primary_clock_index, seeded from the fix's reference system and maintained by report_primary_clock_index), the epoch reference_time the pseudoranges are referenced to — always on the GPS Time count (see VTMember.time_gpst_count), so it does not move when the primary clock changes — how long the filter has been coasting on epochs it could not solve (see run_vt_iteration), and the per-member report of the latest update (member_sats).\n\n\n\n\n\n","title":"GNSSReceiver.VectorTrackingState"},{"category":"type","location":"api/#GNSSReceiver.VTStatus","page":"API Reference","text":"VTStatus\n\nCondensed vector-tracking status carried alongside a receiver snapshot. A nothing in place of a VTStatus means vector tracking is disabled (pure scalar tracking).\n\nFields\n\nrunning::Bool: whether the navigation filter is currently closing the tracking loops. It starts with the first scalar fix and falls back on prolonged measurement starvation.\nmember_sats::Dictionary{Tuple{Symbol,Int},SatInfo}: every member of the loop as of the filter's latest update, keyed exactly as pvt.sats is, each with the position, transmit time and post-fit residuals that update produced for it. Empty while the scalar solve is in control (nothing was filtered), so it never carries a stale report.\nposition_std, clock_std: the filter's own uncertainty in its position (the root sum of the three position variances) and in the reported clock bias. NaN * m while the filter is not running. This is what pvt.dop cannot say: DOP is the geometry of the satellites that were measured, and it is nothing altogether on an epoch carried by the dynamics — where these two are the only quality figures left.\ntime_with_insufficient_meas: how long the filter has been coasting on epochs it could not solve, counted against VectorTracking's insufficient_meas_timeout, at which point vector tracking is abandoned. Nonzero means the loop is on its way out; it is paid back down (at half rate) by solvable epochs.\n\npvt.sats holds the members the update actually measured; member_sats holds those and the coasted ones together, so setdiff(keys(member_sats), keys(pvt.sats)) is what the filter predicted through an obscuration. A coasted member's residual is the divergence indicator: it reduces to the code discriminator for a well-steered member and grows without bound for one drifting away, which is why it is reported rather than dropped.\n\nCurrent loop membership is not carried here: it is the in_vt_loop flags of the snapshot's sat_data, and it describes a different instant than member_sats does — a satellite that has just joined the loop is flagged but has no entry yet, while one released during the latest update still has its entry (with the residual that explains the release) but is no longer flagged. Count the flags where you need the size of the loop.\n\n\n\n\n\n","title":"GNSSReceiver.VTStatus"},{"category":"section","location":"getting_started/#Getting-Started","page":"Getting Started","text":"This page introduces the building blocks of GNSSReceiver.jl and shows the three ways to feed it with samples: a live SDR, a file already stored in the sample element type, and a raw 8-bit offset-binary I/Q recording.\n\nThroughout, receive's second argument is a system: a single GNSS signal such as GPSL1CA(), a CombinedSignal pilot+data pair, or a tuple of these that share one RF band (e.g. (GPSL1CA(), GalileoE1B())) — every constellation in the tuple is acquired, tracked and decoded and all are fused into a single multi-GNSS PVT solution. The examples below use a single system; see Acquisition & Tracking Parameters for the multi-constellation and multi-band forms. Note that the full-CBOC GalileoE1B() needs a sampling frequency above ~12.3 MHz — at lower rates use its BOC(1,1) approximation GalileoE1B_BOC11() (details on the parameters page).","title":"Getting Started"},{"category":"section","location":"getting_started/#The-pipeline-in-one-picture","page":"Getting Started","text":"At the centre of the package is receive. It consumes a measurement channel — a lock-free channel of sample matrices (one column per antenna) — and returns a data channel of ReceiverDataOfInterest snapshots, one per processed chunk:\n\nmeasurement channel ──▶ receive(...) ──▶ data channel ──┬─▶ get_gui_data_channel ─▶ gui\n   (raw samples)                    (PVT + per-sat CN0)  ├─▶ save_data   (to a JLD2 file)\n                                                         └─▶ collect_data (to a Vector)\n\nEverything runs on spawned tasks, so receive returns immediately and the pipeline processes samples as they arrive. A data-channel snapshot carries:\n\nsat_data — a Dictionaries.Dictionary mapping each tracked satellite, keyed by (signal_id, prn) (e.g. (:GPSL1CA, 5)), to its SatelliteDataOfInterest (carrier-to-noise ratio, prompt correlator value, health flag). Keying by (signal_id, prn) lets the same PRN appear on several constellations or bands without colliding, and matches pvt.sats;\npvt — the current PVT solution (position, velocity, time, DOP, …);\nruntime — the elapsed signal time.","title":"The pipeline in one picture"},{"category":"section","location":"getting_started/#Consuming-the-results","page":"Getting Started","text":"You typically hand the data channel to exactly one of three consumers:\n\nConsumer What it does\nget_gui_data_channel → gui Live terminal display (see Graphical User Interface).\nsave_data Consume on a background task and write a JLD2 file when the stream ends.\ncollect_data Block until the stream ends and return a Vector for offline analysis/plotting.\n\nWant more than one? Split the channel with SignalChannels.tee and attach a consumer to each branch (see Graphical User Interface for an example).","title":"Consuming the results"},{"category":"section","location":"getting_started/#1.-Receiving-live-from-an-SDR","page":"Getting Started","text":"The one-call convenience gnss_receiver_gui opens a SoapySDR device, configures it, runs receive and shows the GUI:\n\nusing SoapyRTLSDR_jll          # or SoapyLMS7_jll, SoapyBladeRF_jll, SoapyUHD_jll, …\nusing GNSSReceiver, GNSSSignals, Tracking, SoapySDR, Unitful\n\ngnss_receiver_gui(;\n    system = GPSL1CA(),\n    sampling_freq = 2e6u\"Hz\",\n    chunk_time = 4u\"ms\",           # processing chunk length (tracking granularity)\n    run_time = 40u\"s\",\n    num_ants = Tracking.NumAnts(1),\n    dev_args = first(Devices()),   # pick the first attached device\n)\n\nsystem here can also be a tuple of systems sharing one RF band, e.g. system = (GPSL1CA(), GalileoE1B()), to receive several constellations at once.\n\nTo record raw samples now and process them later, use gnss_write_to_file.","title":"1. Receiving live from an SDR"},{"category":"section","location":"getting_started/#2.-Replaying-a-file-stored-in-the-sample-element-type","page":"Getting Started","text":"If your recording already holds samples you can read with read_files, pass one path per antenna channel. Read them as ComplexF32 so the default CPUThreadedDownconvertAndCorrelator() handles them with no extra configuration:\n\nusing GNSSReceiver, GNSSSignals, Tracking, Unitful\nusing Unitful: Hz, ms\n\nsystem = GPSL1CA()\nsampling_freq = 5e6u\"Hz\"\nfiles = map(i -> \"antenna$i.dat\", 1:4)     # one file per antenna channel\n\n# The chunk length sets the processing/tracking granularity. A few milliseconds is a\n# sensible default.\nnum_samples = Int(upreferred(sampling_freq * 4ms))\n\nmeasurement_channel = read_files(files, num_samples; type = ComplexF32)\n\ndata_channel = receive(\n    measurement_channel,\n    system,\n    sampling_freq;\n    num_ants = NumAnts(4),\n)\n\ngui_channel = get_gui_data_channel(data_channel)\nGNSSReceiver.gui(gui_channel)      # watch the GUI update live\n\nnote: Sample element type and the correlator backend\nreceive picks its downconvert-and-correlator from the default CPUThreadedDownconvertAndCorrelator(), which works with float samples (e.g. ComplexF32). If a recording is stored in an integer type such as Complex{Int16}, read it as ComplexF32 (read_files(...; type = ComplexF32) above) — no per-front-end full-scale value is needed.","title":"2. Replaying a file stored in the sample element type"},{"category":"section","location":"getting_started/#3.-Reading-a-raw-8-bit-offset-binary-recording","page":"Getting Started","text":"Many SDRs and public sample sets store samples as interleaved 8-bit unsigned offset-binary I/Q bytes (128 ≈ zero). read_uint8_iq_file reads that format directly, recentring each byte and emitting baseband samples — so you don't have to write the byte-unpacking loop yourself. Read as ComplexF32 (with center = 127.5 for exact midscale recentring) so the default correlator backend applies:\n\nusing GNSSReceiver, GNSSSignals, Unitful\n\nsampling_freq = 2.048e6u\"Hz\"\nnum_samples = Int(upreferred(sampling_freq * 4u\"ms\"))\n\nmeasurement_channel =\n    read_uint8_iq_file(\"RTLSDR_Bands-L1.uint8\", num_samples; center = 127.5, type = ComplexF32)\n\ndata_channel = receive(measurement_channel, GPSL1CA(), sampling_freq)\n\nresults = collect_data(data_channel)   # gather the whole run for analysis\n\nThe Worked Example (Real Data) uses exactly this path on a public recording and plots a real fix.","title":"3. Reading a raw 8-bit offset-binary recording"},{"category":"section","location":"gui/#Graphical-User-Interface","page":"Graphical User Interface","text":"GNSSReceiver.jl ships a live, interactive terminal dashboard (built on Tachikoma.jl) that updates as the receiver processes samples. It lays out four panels:\n\nCarrier-to-Noise-Density Ratio (CN0) — a bar per tracked satellite (labelled with the RINEX-style satellite id and band, e.g. G05 L1). The colour says whether the satellite is contributing to the fix: green when it is healthy, in lock and its tracking loops have settled enough to range on, yellow when it is healthy but the receiver is not ranging on it — either still settling, normal for roughly the first second after acquisition (see the lock detection section of Acquisition & Tracking Parameters), or a vector-loop member that has lost lock and is being carried through an outage by the navigation filter — and red when the satellite declares itself unhealthy in its navigation message.\nSatellite Direction-of-Arrival — a sky plot of the satellites in view, each drawn at its azimuth/elevation and labelled with its PRN, coloured by constellation (green GPS, blue Galileo, yellow BeiDou), with a legend below. Azimuth runs clockwise from North at the top; the rings are elevation, with the zenith at the centre and the horizon on the rim, so a satellite moves outward as it sets.\nPosition Velocity Time (PVT) — the fix as time (UTC), coordinates, altitude, ground speed and heading, followed by the solution diagnostics (the full DOP breakdown — GDOP/PDOP/HDOP/VDOP/TDOP — inter-system and inter-frequency biases, and pseudorange-residual RMS). Diagnostics are shown by default; press d to hide them.\nMap — the fix on an OpenStreetMap tile (via UnicodeMaps.jl), rendered in the background. This needs network access to fetch tiles; without it (or before the first fix) the panel falls back to the coordinates and a clickable Google Maps link (an OSC 8 terminal hyperlink, so it opens on click in supporting terminals and is copy-pasteable elsewhere).\n\n(Image: The GNSSReceiver terminal GUI)","title":"Graphical User Interface"},{"category":"section","location":"gui/#Keys","page":"Graphical User Interface","text":"Key Action\nq / Ctrl-C quit\nd toggle the PVT diagnostics\n+ / - zoom the map in / out\nh j k l pan the map (west / south / north / east)\n0 recenter the map on the fix and reset the zoom","title":"Keys"},{"category":"section","location":"gui/#Launching-the-GUI","page":"Graphical User Interface","text":"","title":"Launching the GUI"},{"category":"section","location":"gui/#Live-from-an-SDR","page":"Graphical User Interface","text":"The simplest path is gnss_receiver_gui, which opens the device, runs the pipeline and blocks on the GUI until the run finishes:\n\nusing SoapyRTLSDR_jll\nusing GNSSReceiver, GNSSSignals, Tracking, SoapySDR, Unitful\n\ngnss_receiver_gui(;\n    system = GPSL1CA(),\n    sampling_freq = 2e6u\"Hz\",\n    chunk_time = 4u\"ms\",\n    run_time = 40u\"s\",\n    num_ants = Tracking.NumAnts(1),\n    dev_args = first(Devices()),\n)","title":"Live from an SDR"},{"category":"section","location":"gui/#From-a-data-channel","page":"Graphical User Interface","text":"If you already have a data channel from receive (replaying a file, say), wrap it with get_gui_data_channel and pass the result to gui. get_gui_data_channel down-samples the per-chunk stream to a human refresh rate:\n\nusing GNSSReceiver, GNSSSignals, Unitful\n\nmeasurement_channel = read_uint8_iq_file(\n    \"RTLSDR_Bands-L1.uint8\",\n    Int(upreferred(2.048e6u\"Hz\" * 4u\"ms\"));\n    center = 127.5,\n    type = ComplexF32,\n    sample_rate = 2.048e6u\"Hz\",    # replay in real time so the dashboard is watchable\n)\ndata_channel = receive(\n    measurement_channel, GPSL1CA(), 2.048e6u\"Hz\";\n    pvt_approximate_year = 2017,   # this recording is from 2017 (GPS week rollover)\n)\n\nGNSSReceiver.gui(get_gui_data_channel(data_channel))   # press `q` to quit\n\nQuitting with q closes the GUI channel, and that close propagates back up the pipeline: receive stops processing and the file reader stops reading. So q ends the run — it does not leave the receiver grinding through the rest of the recording in the background.\n\nFor a complete, copy-pasteable walkthrough — downloading a public recording and replaying it into the dashboard — see the Worked Example (Real Data) page.","title":"From a data channel"},{"category":"section","location":"gui/#Showing-the-GUI-*and*-saving-the-data","page":"Graphical User Interface","text":"The GUI is just one consumer of the data channel. To display it and persist the run, split the channel with tee and give each branch its own consumer:\n\nusing SignalChannels: tee\n\ndata_channel1, data_channel2 = tee(data_channel)\ndata_task = save_data(data_channel1; filename = \"run.jld2\")\n\ngui_channel = get_gui_data_channel(data_channel2)\nGNSSReceiver.gui(gui_channel)\n\nwait(data_task)   # `save_data` returns its writer task; wait until the file is on disk","title":"Showing the GUI and saving the data"},{"category":"section","location":"gui/#Refresh-rate","page":"Graphical User Interface","text":"gui accepts an fps keyword (default 12) that sets the dashboard's redraw rate; the underlying GUIData stream is already down-sampled to a human refresh rate by get_gui_data_channel. For the smoothest display, start Julia with an interactive thread (julia -t auto,1) — gui runs its render loop on the interactive threadpool when one is available, so it is never starved by the streaming/DSP work. See the docstrings for get_gui_data_channel and gui in the API Reference.","title":"Refresh rate"},{"category":"section","location":"hardware_contract/#Hardware-correlator-contract","page":"Hardware-Correlator Contract","text":"This page is the contract between GNSSReceiver and a hardware correlator — an SDR whose FPGA downconverts and correlates on-device. Its tracking loops are closed by a separate, allocation-free loop process built on HardwareLoopCore.jl over a vendor driver; the receiver talks to that process through a HardwareLoopProtocol.jl shared-memory segment and never sees a correlator dump or writes an NCO word itself. What a vendor package still describes to this package is the device: its raw sample streams, its declared capabilities and RF band plan, its replica amplitudes and the origin of its sample counter. That is what this page specifies. The types are documented in the API reference; this page says what they oblige you to do.\n\nThe split is described under AbstractHardwareCorrelatorSDR: the device's raw sample stream keeps driving acquisition, decoding, PVT and the runtime clock exactly as in the software receiver, and only the correlation and the loop closure move — to the device and its loop process.","title":"Hardware-correlator contract"},{"category":"section","location":"hardware_contract/#0.-The-three-halves-of-a-vendor-package","page":"Hardware-Correlator Contract","text":"Half Lives in Talks to\nDriver — HardwareLoopCore.AbstractLoopDriver: read records off the device, write NCO words, arm and release channels, report the device sample count and what the gateware can do the loop process (e.g. GNSSM2SDR's M2SDRLoop, built with juliac --trim=safe) the FPGA\nDevice — AbstractHardwareCorrelatorSDR: this page the receiver process GNSSReceiver\nLoop handle — RemoteHardwareLoop(sdr; segment, spawn) the receiver process the loop process over the segment\n\nThe receiver's entry point is\n\nloop = RemoteHardwareLoop(sdr; segment = \"/dev/shm/gnss-loop-m2sdr0\", spawn = `gnss_loop …`)\ndata = receive(loop, GPSL1CA(), 4e6u\"Hz\")\n\nA vendor package wraps this — GNSSM2SDR's M2SDRRemote and remote_loop do — so a user names a CSR map, a raw stream and a sample rate and gets the loop handle back.\n\nThe driver's obligations (record format, latest-first accumulators, one record per primary code period, sample-exact scheduled arms and words, strobes) are HardwareLoopCore's contract and documented there. Everything below is the device's.","title":"0. The three halves of a vendor package"},{"category":"section","location":"hardware_contract/#1.-Declare-what-the-device-can-do","page":"Hardware-Correlator Contract","text":"GNSSReceiver.hardware_capabilities(sdr::MyDevice) = HardwareCorrelatorCapabilities(;\n    signals = [:GPSL1CA, :GalileoE1B, :GalileoE1C],\n    modulations = [:LOC, :CBOC],\n    max_primary_code_length = 4092,\n    code_frequency_limits = (1.023e6, 1.023e6),\n    tap_layouts = [3, 5],\n    max_tap_offset_chips = 1.0,\n    num_antennas = 1,\n    bands = [:L1],\n    num_rf_inputs = 1,\n    max_secondary_code_length = 1,\n    reports_code_phase = true,\n)\n\nA device that implements nothing is taken to be LEGACY_GPS_L1CA_CAPABILITIES — the GPS L1 C/A correlator this interface was originally written against. That is the compatibility path: an adapter written before this interface existed keeps working unchanged, and keeps being described truthfully.\n\nDeclare every field honestly. An under-declared capability is one the receiver will refuse to use; an over-declared one is a channel that arms and never locks, which is far harder to attribute. The loop process's driver declares the same facts on its side (driver_capabilities) and refuses an arm it cannot serve; the two declarations should be read off the same gateware registers.","title":"1. Declare what the device can do"},{"category":"section","location":"hardware_contract/#2.-Nothing-is-armed-before-it-is-validated","page":"Hardware-Correlator Contract","text":"receive(::RemoteHardwareLoop, systems, sampling_freq; …) calls validate_hardware_configuration before it starts a task and before a single arm command is published. Every component of every configured system is checked against the declaration above, and every problem is reported in one ArgumentError:\n\nMyDevice cannot track GalileoE1B on this hardware correlator:\n  - the device has no code generator for GalileoE1B (it declares GPSL1CA)\n  - the device cannot synthesise CBOC modulation (it declares LOC)\n  - the primary code is 4092 chips, past the device's 1023-chip code memory\n  - GalileoE1B is tracked with a 5-tap VeryEarlyPromptLateCorrelator, and the\n    device's correlator bank produces 3-tap layouts\n\nUse check_hardware_support for a single signal, and GNSSReceiver.hardware_support_error when you want the reasons as a string rather than an exception.\n\nreceive also checks the loop process itself: the segment's band table has to carry every requested band at the requested sample rate, and vector tracking — which closes the loops on the host — is refused.\n\nThe same per-signal check runs again for every arm, in the loop process (REJECT_UNSUPPORTED): there it refuses the channel and the receiver counts it in its unsupported_signals instead of throwing, because that code runs on the chunk path, where taking the receiver down would cost every other satellite its lock.","title":"2. Nothing is armed before it is validated"},{"category":"section","location":"hardware_contract/#3.-The-raw-stream-and-the-device-counter","page":"Hardware-Correlator Contract","text":"raw_sample_channel(sdr) — per band, (sdr, band_id) — is the raw I/Q the receiver acquires on and clocks itself by. It must keep running for the whole session: on the LiteX-M2SDR the tracking bank observes the RX datapath non-intrusively and only sees samples while the raw DMA drains.\n\ndevice_sample_origin(sdr, band_id) is the device counter reading that corresponds to host raw sample 0 of that band. An acquisition yields a code phase at a host sample; the receiver adds this origin before it asks the loop process to arm a channel at that device sample. The default is 0, right for a device whose counter starts with the stream (and for the simulated device); a real device latches its counter when the raw stream starts — GNSSM2SDR does it in latch_origin!, after the first raw chunk has arrived. Get this wrong by n samples and every handover is n samples off in code phase: a handover error past about half a chip never pulls in.","title":"3. The raw stream and the device counter"},{"category":"section","location":"hardware_contract/#4.-What-the-loop-process-gets-per-arm","page":"Hardware-Correlator Contract","text":"The receiver builds one arm command per tracked signal component from the tracking state, and it is the same information the in-process link used to hand a device: the signal and PRN, the acquisition's Dopplers and code phase with the device sample they are valid at, every quantised tap offset in whole input samples (latest first, prompt at zero — see below), the replica normalisation (section 5), :primary_only as the secondary-code mode (section 7) and the band. The receiver arms one noise reference per band as well: an open-loop despread on an unused PRN of the band's reference signal, which is what the C/N₀ estimator divides by.","title":"4. What the loop process gets per arm"},{"category":"section","location":"hardware_contract/#Why-all-the-tap-offsets,-and-not-just-the-spacing","page":"Hardware-Correlator Contract","text":"Tracking's discriminators do not use the spacing the host programmed: they recover it from the correlator handed back to them, through that correlator's preferred code shifts. So the device has to reproduce the host's quantisation, not its own. For a three-tap bank getting this wrong is a DLL loop-gain error (~2.3 % at 4 MHz and a 0.5-chip preferred shift). For a five-tap bank it is worse: the VE/VL distance enters the discriminator separately, and there is no single number to re-derive it from. The arm command carries the array — program it.","title":"Why all the tap offsets, and not just the spacing"},{"category":"section","location":"hardware_contract/#5.-Amplitude-normalisation,-per-band-and-per-signal","page":"Hardware-Correlator Contract","text":"The loop brings every accumulator onto one scale — the scale the software correlator would have produced from the same samples — by dividing by\n\nreplica_amplitude × code_amplitude / GNSSSignals.get_code_amplitude(signal)\n\nBoth factors come from the device:\n\ncorrelator_gain(sdr, band_id) — the amplitude of the carrier replica the gateware wipes off with, relative to a unit-amplitude replica. A device mixing with a ±127 sine/cosine table returns 127. Declare it per band: a multi-band front end rarely has one gain chain, and the error is a flat 20·log10(g) dB offset on that band's C/N₀ — exactly the kind of bias a lock-detector threshold silently absorbs.\nreplica_code_amplitude(sdr, signal) — the RMS amplitude of the code replica, on the scale GNSSSignals.get_code_amplitude reports. The default says the device reproduces the modelled code exactly, which is true for every ±1 code. Override it where the gateware approximates: a device replicating Galileo E1B with a plain ±1 BOC(1,1) replica has an amplitude of 1 where the modelled CBOC table has ≈ 19.92, and without the override the same satellite reads ~26 dB apart depending on which correlator produced it.\n\nBoth are read once per assignment and frozen into the arm command, so they may be computed rather than stored.\n\nThe discriminators are ratios and cannot see any of this; the noise-referenced C/N₀ estimator can, because it divides the prompt power by a floor measured elsewhere — and the receiver's code-lock threshold (30 dBHz) reads that number. That is why it has to be declared rather than left at the default.","title":"5. Amplitude normalisation, per band and per signal"},{"category":"section","location":"hardware_contract/#5a.-RF-bands,-inputs-and-the-receiver-timebase","page":"Hardware-Correlator Contract","text":"Supporting every signal is not the same as receiving every RF band at once. The first is a property of the correlator; the second is a property of the front end, and this section is where it is declared, checked and — where it is exceeded — refused.","title":"5a. RF bands, inputs and the receiver timebase"},{"category":"section","location":"hardware_contract/#Declare-where-each-band-arrives","page":"Hardware-Correlator Contract","text":"GNSSReceiver.band_rf_input(sdr::MyDevice, band_id) = band_id === :L1 ? 1 : 2\nGNSSReceiver.band_device_index(sdr::MyDevice, band_id) = 1\nGNSSReceiver.clock_synchronization(sdr::MyDevice) = :single_device\nGNSSReceiver.band_hardware_channels(sdr::MyDevice, band_id) =\n    band_id === :L1 ? (1:10) : (11:20)\nGNSSReceiver.raw_sample_channel(sdr::MyDevice, band_id) = sdr.raw[band_id]\nGNSSReceiver.device_sample_origin(sdr::MyDevice, band_id) = sdr.origin[band_id]\n\nEverything here has a single-band default, so a one-band adapter implements none of it. A device that receives several bands at once implements all of it, and the receiver builds a HardwareBandPlan from it (hardware_band_plan) — one HardwareBandRoute per band, carrying the band's RF input, its device and its sample rate. The loop process's segment carries the same band table (band ids and sample rates), and receive refuses a request the two disagree on.\n\nConcept What it counts Where it is declared\nRF input Independently tuned bands received at once band_rf_input, capped by HardwareCorrelatorCapabilities.num_rf_inputs\nAntenna Coherent chains of one band HardwareCorrelatorCapabilities.num_antennas\nCorrelator bank The channels that can see a given band band_hardware_channels\n\nThese are three different things and none substitutes for another. A four-antenna L1 front end is one route with num_antennas = 4: the antennas share one LO and one sample clock, and the bank despreads all of them into one accumulator vector per tap so the host can beamform from the prompt covariance. Two bands share nothing but the board.","title":"Declare where each band arrives"},{"category":"section","location":"hardware_contract/#Per-band-sample-rates-and-the-one-receiver-timebase","page":"Hardware-Correlator Contract","text":"Each band's raw samples, each record and each NCO word are counted on that band's own counter, at that band's own rate. The receiver folds, ranges and schedules on one axis instead: the reference band — the first route of the plan — whose counter is the receiver timebase. receive takes that band's rate as its sampling_freq and the others alongside it:\n\nreceive(loop, ((GPSL1CA(),), (BeiDouB1I(),)), (4e6u\"Hz\", 5e6u\"Hz\"))\n\nThe mapping between the axes is the exact ratio of the two rates — no offset, no estimate, no drift — so one millisecond is 4000 counts on a 4 MS/s band and 5000 on a 5 MS/s one, and both close the same epoch. A handover's valid_at_sample is written on the channel's own band counter (plus that band's device_sample_origin), and every satellite's code phase is extrapolated to the same instant on its own band's axis, which is the common reception time the multi-band pseudoranges are differences of.\n\nFor a single-band device every scale is exactly 1.0, both mappings are the identity, and nothing above changes anything.","title":"Per-band sample rates and the one receiver timebase"},{"category":"section","location":"hardware_contract/#Multi-device-clock-synchronisation","page":"Hardware-Correlator Contract","text":"clock_synchronization says what makes counters on different devices comparable:\n\nValue Meaning Supported\n:single_device One device, one sample clock yes (the default)\n:shared_clock Several devices on one reference and one distributed sample clock, counters aligned at start accepted by the validation\n:independent Free-running clocks refused for a multi-device plan\n\n:independent is refused rather than approximated. Two free-running clocks drift by parts per million against each other — metres of pseudorange per second — and the ratio mapping above has no offset term to absorb it, so there is no common reception epoch and a fix built from both bands would be wrong with nothing looking wrong. A receiver that needs it has to estimate and steer the inter-device offset first, which this package does not do.","title":"Multi-device clock synchronisation"},{"category":"section","location":"hardware_contract/#What-happens-when-the-request-exceeds-the-front-end","page":"Hardware-Correlator Contract","text":"validate_hardware_configuration checks the plan as a whole, before anything is armed, and reports every problem at once (GNSSReceiver.band_plan_error):\n\na band the front end does not declare it can tune;\nmore bands on one device than it has RF inputs;\ntwo bands routed to one RF input;\na multi-device plan whose clocks are :independent;\na band whose correlator bank is empty, or two bands claiming one channel (GNSSReceiver.band_bank_error) — a band the receiver can tune and then never track anything on is the same silent partial configuration.\n\nThere is no automatic fallback to sequential retuning — receiving band A for a while, then retuning to band B. It would turn a simultaneous request into a time-multiplexed one behind the caller's back, changing every band's C/N₀, its measurement epochs and its fix rate, and leaving no band continuously tracked. A receiver that wants it runs one receive per band configuration and retunes between them, which is explicit; the refusal message says so.","title":"What happens when the request exceeds the front end"},{"category":"section","location":"hardware_contract/#Per-band-noise-references-and-replica-gain","page":"Hardware-Correlator Contract","text":"A noise density belongs to one front end's gain chain, antenna, filter and interference environment, and to the modulation that despreads it. Two bands share none of those, so the receiver arms one noise reference per band in the loop process and never pools them, and correlator_gain(sdr, band_id) is read per band, so a band whose replica table is scaled differently still lands on the same C/N₀ scale. Pooling two floors would describe neither band, and the error lands on every satellite of both as a C/N₀ bias — the quantity the code lock detector thresholds on, and the one number in the receiver that nothing downstream can contradict.","title":"Per-band noise references and replica gain"},{"category":"section","location":"hardware_contract/#Measured:-what-the-LiteX-M2SDR-front-end-can-actually-do","page":"Hardware-Correlator Contract","text":"The reference device for this contract is a one-band front end, and that is worth stating precisely rather than leaving to be discovered. Read off the board (orin2, gateware LiteX-M2SDR SoC / m2 variant / built on 2026-07-29, 20-channel GNSS build) on 2026-09-16:\n\nthe RF configuration registers are singular: one ad9361_active_rx_frequency_khz, one ad9361_active_sample_rate, one ad9361_active_bandwidth. There is no per-input frequency or rate register anywhere in the CSR map.\nthe only per-RX-chain registers are the AGC saturation counters (ad9361_agc_count_rx1_*, ad9361_agc_count_rx2_*), and the RF utility exposes --rx-gain1 / --rx-gain2 against a single --rx-freq and a single --sample-rate. RX1 and RX2 are two coherent antenna chains behind one AD9361 RX LO and one sample clock — num_antennas = 2, num_rf_inputs = 1.\nnone of the 20 correlator channels (gnss_ch0…gnss_ch19) carries a band, RF input or antenna selector: one bank, fed from one datapath.\nretuning is device-wide. Writing L5 into that one register moved the whole front end (0x001809fc = 1 575 420 kHz → 0x0011f382 = 1 176 450 kHz) and writing L1 back moved it all the way back; nothing stayed on L1 in between.\n\nSo a supported multi-band configuration cannot be demonstrated on this hardware, and this page does not claim one. An M2SDR adapter should declare num_rf_inputs = 1 and whatever num_antennas its build has, and a two-band request will then be refused before arming with the message in GNSSReceiver.band_plan_error — which is the correct outcome for this front end, not a limitation of the routing. The multi-band routing was validated in simulation on the in-process link this loop process replaced; the loop process has so far run one band only, and a multi-band run through it awaits a front end with two independently tuned inputs.","title":"Measured: what the LiteX-M2SDR front end can actually do"},{"category":"section","location":"hardware_contract/#Supported-configurations,-today","page":"Hardware-Correlator Contract","text":"Configuration Status\nOne band, one or more coherent antennas Supported; the path every existing adapter is on\nSeveral bands on one device Accepted by the validation; not yet run through the loop process\nSeveral bands over several devices on a shared reference and sample clock Accepted by the validation; not demonstrated on hardware\nTwo bands on the LiteX-M2SDR Impossible: one RX LO, one sample clock (measured above)\nSeveral bands over devices on independent clocks Refused before arming\nMore bands than RF inputs, by sequential retuning Not performed; refused before arming","title":"Supported configurations, today"},{"category":"section","location":"hardware_contract/#6.-Carrier-phase-conventions-for-components","page":"Hardware-Correlator Contract","text":"A satellite's components share one carrier. The receiver locks the driver component (the first of tracking_signals, i.e. the pilot for a pilot/data pair) onto the real axis and de-rotates every other component by its own ICD phase offset — GNSSSignals.get_carrier_phase_offset, e.g. −π/2 for GPS L5-Q against L5-I — so a quadrature component does not decode off the collapsed real part.\n\nThat only works if the phase relationship survives into the accumulators. So:\n\nMix every channel of one band against one common in-phase carrier reference, at the band's carrier, with no per-component rotation.\nThe component's offset against that reference is there to be preserved, not applied. A device that unavoidably rotates per component must remove exactly this value again before dumping.\nDo not pre-combine components in hardware, and do not pre-combine antennas: beamforming is post-correlation on the host and adapts from the per-antenna prompt covariance, so an N-antenna device dumps one accumulator per antenna per tap.","title":"6. Carrier-phase conventions for components"},{"category":"section","location":"hardware_contract/#7.-Secondary-codes","page":"Hardware-Correlator Contract","text":"The loop process removes the overlay; the device replicates the primary code. That is the ownership decision, stated per arm as the secondary-code mode: it is :primary_only for every device and every signal, which is also the protocol's own default. A device is never asked to wipe an overlay, so the overlay is removed exactly once — in the loop core's ingest, before the coherent pre-accumulation and therefore before the discriminators, the C/N₀ estimator and the navigation bit accumulation alike.\n\nWhy not the device: an overlay's phase is not known when a channel is armed. It is recovered by the bit/secondary-code sync detector, some way into tracking, from the prompts themselves, and a device asked to wipe at the wrong phase cancels the signal instead of accumulating it. Handing the job to the gateware therefore needs a scheduled command (\"from device sample n, overlay chip k\") on the same sample-exact footing as an NCO word, plus a way to read the device's overlay counter back so that a lost record cannot leave the two ends disagreeing in silence. That contract is not defined yet.\n\nmax_secondary_code_length therefore stays a declaration — the longest overlay the gateware could wipe if it were asked — and 1, the default, is not an error and costs a device nothing. It is what supports_secondary_code_wipeoff reads, and nothing requests it.\n\nRemoving secondary codes after synchronisation is issue #132. It was demonstrated end to end for GPS L5I on the in-process link this loop process replaced; the loop core carries the same ingest path, and the demonstration through the loop process is the follow-up recorded in the signal support matrix.","title":"7. Secondary codes"},{"category":"section","location":"hardware_contract/#8.-Liveness,-gaps-and-restarts","page":"Hardware-Correlator Contract","text":"The loop process heartbeats into the segment; RemoteHardwareLoop checks it every chunk. A loop whose heartbeat stops for heartbeat_timeout seconds is restarted from its spawn command and every tracked satellite re-armed from the receiver's own state; a receiver attaching to a loop that is already running releases every channel first and starts from a clean bank.\n\nA satellite whose records were delayed on their way from the device is not a satellite that lost the signal: is_observation_gap freezes its lock detectors through a gap of up to max_dump_gap seconds instead of letting them decay, and has_current_observations keeps a frozen bit count out of navigation meanwhile. Past the budget the path is not late but broken, and the detectors are allowed to decay so the satellite is eventually released rather than held in a lock nothing confirms. When the loop process has to restart a satellite's bit clock (records were lost on the device), the receiver learns it through take_bit_clock_restart! and restarts the decoder.","title":"8. Liveness, gaps and restarts"},{"category":"section","location":"hardware_contract/#9.-Adapter-checklist","page":"Hardware-Correlator Contract","text":"[ ] raw_sample_channel and num_hardware_channels\n[ ] hardware_capabilities, declared field by field and agreeing with the driver's driver_capabilities\n[ ] device_sample_origin, latched when the raw stream starts\n[ ] correlator_gain per band, and replica_code_amplitude wherever the gateware approximates a code\n[ ] For a multi-band device: band_rf_input, band_hardware_channels, raw_sample_channel(sdr, band_id), device_sample_origin(sdr, band_id) and — across devices — band_device_index and clock_synchronization (section 5a)\n[ ] A driver for the loop process (HardwareLoopCore's AbstractLoopDriver) and an executable that runs LoopCore over it, plus the spawn command a RemoteHardwareLoop starts it with","title":"9. Adapter checklist"},{"category":"section","location":"hardware_contract/#10.-What-this-contract-does-not-yet-cover","page":"Hardware-Correlator Contract","text":"Extending the hardware path from GPS L1 C/A to every GNSSSignals signal is tracked by issue #130; the parts still to land are a scheduled hardware secondary-code wipeoff (#132 removes the overlay in the loop process; the device-side contract is sketched in section 7), the gateware and adapter halves of records shorter than a primary period (the loop core accounts for them; the gateware is gnss-m2sdr#29 and the adapter GNSSM2SDR.jl#8), primary codes longer than a device's code memory, and the per-signal validation matrix (#135). Routing channels and noise estimates across RF bands (#134) is specified in section 5a and checked by the validation; what is not yet demonstrated is a multi-band run through the loop process, and multi-device operation on a shared sample clock is accepted by the validation without having been run. Adopting a running loop's satellites on attach, rather than releasing them, is not implemented either. Until the rest lands, declare conservatively: a capability you cannot serve is a channel that never locks.","title":"10. What this contract does not yet cover"},{"category":"section","location":"example/#Worked-Example-(Real-Data)","page":"Worked Example (Real Data)","text":"This page runs the complete receiver pipeline on a real GNSS recording and computes an actual position fix. Every code block below is executed when the documentation is built, so the numbers and plots you see were produced by GNSSReceiver.jl on live data — not hand-written.\n\nThe recording is the public ION SDR sample data: 8-bit unsigned offset-binary I/Q at 2.048 MS/s, GPS L1, captured on 2017-09-10 in Oegstgeest, NL. It is the same signal used by the package's integration test.","title":"Worked Example (Real Data)"},{"category":"section","location":"example/#Download-the-recording","page":"Worked Example (Real Data)","text":"We only need the first ~47 seconds to acquire the satellites and decode enough of the navigation message for a fix, so we fetch a prefix of the file with an HTTP range request (≈ 193 MB) rather than the full 60-second, ~246 MB recording.\n\nusing Downloads\n\nurl = \"https://sdr.ion.org/RTL_SDR/RTLSDR_Bands-L1.uint8\"\nfile = joinpath(tempdir(), \"RTLSDR_Bands-L1_prefix.uint8\")\nnbytes = 193_000_000\n\nif !isfile(file) || filesize(file) < nbytes\n    Downloads.download(url, file; headers = [\"Range\" => \"bytes=0-$(nbytes - 1)\"])\nend\nfilesize(file)\n193000000","title":"Download the recording"},{"category":"section","location":"example/#Run-the-receiver","page":"Worked Example (Real Data)","text":"Reading the raw offset-binary file, running the pipeline and collecting the results is three calls — read_uint8_iq_file, receive and collect_data:\n\nusing GNSSReceiver, GNSSSignals, Unitful\n\nsampling_freq = 2.048e6u\"Hz\"\nsystem = GPSL1CA()\nnum_samples = Int(upreferred(sampling_freq * 4u\"ms\"))   # 4 ms chunks\n\nmeasurement_channel =\n    read_uint8_iq_file(file, num_samples; center = 127.5, type = ComplexF32)\n\ndata_channel = receive(\n    measurement_channel,\n    system,\n    sampling_freq;\n    pvt_approximate_year = 2017,   # resolves the GPS week-number rollover for old data\n)\n\nresults = collect_data(data_channel)\nlength(results)   # one snapshot per `pvt_update_interval`, plus the final state\n462\n\nnote: Reading the raw offset-binary file\nThe raw file stores each I and Q value as an 8-bit unsigned byte (0…255) in offset-binary form: the zero-signal level sits at the middle of the range, 128, rather than at 0. So a byte of 128 means ≈ zero amplitude, 255 means maximum positive and 0 means maximum negative.read_uint8_iq_file turns that into normal signed baseband samples by recentring — subtracting center from every byte. Reading as ComplexF32 with center = 127.5 recentres exactly on midscale and produces float baseband samples, so receive's default CPUThreadedDownconvertAndCorrelator() processes them with no extra configuration.\n\nresults is a Vector of ReceiverDataOfInterest snapshots. The last one is the final state of the receiver:\n\nfinal = last(results)\nfinal.runtime\n47.115999999995644 s","title":"Run the receiver"},{"category":"section","location":"example/#Carrier-to-noise-ratio-per-satellite","page":"Worked Example (Real Data)","text":"The estimated CN0 of every tracked satellite — this is the same information the live GUI shows as a bar chart:\n\nsat_data is a Dictionaries.Dictionary keyed by (signal_id, prn), so iterate it with pairs:\n\nusing UnicodePlots\n\nlabels = String[]\ncn0s = Float64[]\nfor ((signal_id, prn), sd) in pairs(final.sat_data)\n    push!(labels, string(signal_id, \" \", prn))\n    push!(cn0s, ustrip(sd.cn0))\nend\n\nbarplot(\n    labels,\n    round.(cn0s; digits = 1);\n    xlabel = \"CN0 [dBHz]\",\n    ylabel = \"Satellite\",\n    title = \"Carrier-to-Noise-Density Ratio\",\n)\n                              \u001b[97;1mCarrier-to-Noise-Density Ratio\u001b[0m       \n                        \u001b[38;5;8m┌                                        ┐\u001b[0m \n              GPSL1CA 5 \u001b[38;5;8m┤\u001b[0m\u001b[38;5;2m■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■\u001b[0m 51.9 \u001b[38;5;8m \u001b[0m \u001b[38;5;8m\u001b[0m\n             GPSL1CA 13 \u001b[38;5;8m┤\u001b[0m\u001b[38;5;2m■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■\u001b[0m 47      \u001b[38;5;8m \u001b[0m \u001b[38;5;8m\u001b[0m\n             GPSL1CA 15 \u001b[38;5;8m┤\u001b[0m\u001b[38;5;2m■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■\u001b[0m 48.6   \u001b[38;5;8m \u001b[0m \u001b[38;5;8m\u001b[0m\n             GPSL1CA 20 \u001b[38;5;8m┤\u001b[0m\u001b[38;5;2m■■■■■■■■■■■■■■■■■■■■■■■■■■■■\u001b[0m 43.5       \u001b[38;5;8m \u001b[0m \u001b[38;5;8m\u001b[0m\n             GPSL1CA 21 \u001b[38;5;8m┤\u001b[0m\u001b[38;5;2m■■■■■■■■■■■■■■■■■■■■■■■■■■■■\u001b[0m 43.1       \u001b[38;5;8m \u001b[0m \u001b[38;5;8m\u001b[0m\n   Satellite GPSL1CA 28 \u001b[38;5;8m┤\u001b[0m\u001b[38;5;2m■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■\u001b[0m 48.9   \u001b[38;5;8m \u001b[0m \u001b[38;5;8m\u001b[0m\n             GPSL1CA 30 \u001b[38;5;8m┤\u001b[0m\u001b[38;5;2m■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■\u001b[0m 48.9   \u001b[38;5;8m \u001b[0m \u001b[38;5;8m\u001b[0m\n             GPSL1CA 18 \u001b[38;5;8m┤\u001b[0m\u001b[38;5;2m■■■■■■■■■■■■■■■■■■■■■■■■■■■\u001b[0m 41          \u001b[38;5;8m \u001b[0m \u001b[38;5;8m\u001b[0m\n             GPSL1CA 24 \u001b[38;5;8m┤\u001b[0m\u001b[38;5;2m■■■■■■■■■■■■■■■■■■■■■■■■■■■\u001b[0m 41.5        \u001b[38;5;8m \u001b[0m \u001b[38;5;8m\u001b[0m\n              GPSL1CA 7 \u001b[38;5;8m┤\u001b[0m\u001b[38;5;2m■■■■■■■■■■■■■■■■■■■■■■■■■■■■\u001b[0m 42         \u001b[38;5;8m \u001b[0m \u001b[38;5;8m\u001b[0m\n              GPSL1CA 8 \u001b[38;5;8m┤\u001b[0m\u001b[38;5;2m■■■■■■■■■■■■■■■■■■■■■■■■■■\u001b[0m 39.9         \u001b[38;5;8m \u001b[0m \u001b[38;5;8m\u001b[0m\n                        \u001b[38;5;8m└                                        ┘\u001b[0m \n                                        CN0 [dBHz]                 ","title":"Carrier-to-noise ratio per satellite"},{"category":"section","location":"example/#The-position-fix","page":"Worked Example (Real Data)","text":"Ephemeris decoding takes about half a minute, so the first PVT solution appears roughly 35 seconds into the recording. Here is the final fix:\n\nusing PositionVelocityTime: get_LLA\n\nlla = get_LLA(final.pvt)\n\nprintln(\"Time (TAI):        \", final.pvt.time)\nprintln(\"Latitude:          \", lla.lat)\nprintln(\"Longitude:         \", lla.lon)\nprintln(\"Altitude:          \", round(lla.alt; digits = 1), \" m\")\nprintln(\"Satellites in fix: \", length(final.pvt.sats))\nprintln(\"Position DOP:      \", round(final.pvt.dop.PDOP; digits = 2))\nTime (TAI):        2017-09-10T22:57:07.801 TAI\nLatitude:          52.17722359232535\nLongitude:         4.490060205704025\nAltitude:          49.3 m\nSatellites in fix: 11\nPosition DOP:      1.45\n\nThe recording was made at roughly 52.177° N, 4.490° E in Oegstgeest, Netherlands — which is exactly what the receiver reports. You can drop those coordinates straight into a map:\n\nprintln(\"https://www.google.com/maps/search/$(ustrip(lla.lat)),$(ustrip(lla.lon))\")\nhttps://www.google.com/maps/search/52.17722359232535,4.490060205704025","title":"The position fix"},{"category":"section","location":"example/#There's-more-in-each-snapshot","page":"Worked Example (Real Data)","text":"The summary is compact, but the PVT solution it carries is the full PositionVelocityTime.PVTSolution, so a lot more is available from the same snapshot without any extra work:\n\npvt = final.pvt\nprintln(\"Velocity (ECEF):     \", round.(pvt.velocity; digits = 2), \" m/s\")\nprintln(\"Clock drift:         \", pvt.relative_clock_drift)\nprintln(\"GDOP / HDOP / VDOP:  \", round(pvt.dop.GDOP; digits = 2), \" / \",\n        round(pvt.dop.HDOP; digits = 2), \" / \", round(pvt.dop.VDOP; digits = 2))\n# `sat_data` is keyed by (signal_id, prn):\nsome_key = first(keys(final.sat_data))\nprintln(\"Prompt ($some_key):  \", final.sat_data[some_key].prompt)\nVelocity (ECEF):     [0.06, -0.28, 3.03] m/s\nClock drift:         7.339002862115127e-7\nGDOP / HDOP / VDOP:  1.62 / 0.91 / 1.13\nPrompt ((:GPSL1CA, 5)):  -71386.95263671875 + 5856.454010009766im\n\nIf you need a quantity that the summary does not carry at all — the raw carrier Doppler, code phase or carrier phase of the tracking loops, or the decoded navigation data — you can have receive emit a payload of your own. See Custom Receiver Output.","title":"There's more in each snapshot"},{"category":"section","location":"example/#Watch-it-live-in-the-terminal","page":"Worked Example (Real Data)","text":"To watch this unfold live instead of post-processing it, hand the data channel to the live Graphical User Interface instead of collect_data. Using the same file and settings as above:\n\nusing GNSSReceiver, GNSSSignals, Unitful\n\nsampling_freq = 2.048e6u\"Hz\"\nnum_samples = Int(upreferred(sampling_freq * 4u\"ms\"))\n\n# `sample_rate` paces the file replay to real time, so the dashboard updates at the speed\n# the signal was recorded instead of flashing past and closing immediately.\nmeasurement_channel = read_uint8_iq_file(\n    file, num_samples; center = 127.5, type = ComplexF32, sample_rate = sampling_freq)\ndata_channel = receive(\n    measurement_channel, GPSL1CA(), sampling_freq; pvt_approximate_year = 2017)\n\n# Opens the interactive dashboard (CN0 bars, sky plot, PVT, map). Press `q` to quit.\nGNSSReceiver.gui(get_gui_data_channel(data_channel))\n\nThis block is not run when the docs are built because the dashboard takes over the terminal. Run it in a real terminal — ideally with an interactive thread (julia -t auto,1) so the render loop is never starved. With sample_rate set the replay runs at real time, so the fix appears after the receiver has decoded enough of the navigation message (~35 s in), and the final frame stays on screen until you press q.","title":"Watch it live in the terminal"},{"category":"section","location":"parameters/#Acquisition-and-Tracking-Parameters","page":"Acquisition & Tracking Parameters","text":"Almost everything about how the receiver behaves is controlled through keyword arguments to receive (and, for the live path, gnss_receiver_gui). This page groups those keywords by pipeline stage and shows how to set them. The full, authoritative list lives in the receive docstring at the bottom of the page.","title":"Acquisition & Tracking Parameters"},{"category":"section","location":"parameters/#Systems:-multi-constellation-and-multi-band","page":"Acquisition & Tracking Parameters","text":"receive's second argument selects which signals to receive:\n\nA single system — one GNSS signal (GPSL1CA()), a CombinedSignal pilot+data pair, or a tuple of these — that shares one RF band:\nreceive(measurement_channel, GPSL1CA(), sampling_freq)\nA tuple of systems sharing one RF band, fused into a single multi-GNSS PVT solution. GPS L1 C/A and Galileo E1 both live on L1, so they can share one stream:\nreceive(measurement_channel, (GPSL1CA(), GalileoE1B()), sampling_freq)\n(All systems in the tuple must share one RF band; mixing bands — e.g. L1 and L5 — throws.)\nnote: Galileo E1B needs a high sampling frequency — or its BOC(1,1) approximation\nGalileoE1B() is the full CBOC(6,1,1/11) signal, whose BOC(6,1) component requires a sampling frequency above ~12.3 MHz (Nyquist for the 6.138 MHz subcarrier). At typical low SDR rates — like the 2.048 MHz used in the examples on these pages — use GalileoE1B_BOC11() instead, the BOC(1,1) approximation that ignores the BOC(6,1) component and correlates fine at low rates (at a small C/N₀ penalty).\nFor several RF bands, use the multi-band method: a tuple of measurement channels (one per band), a tuple of per-band system groups and a tuple of interm_freqs, all aligned band-by-band. Every band is fused into one solution with per-constellation clock biases and per-band inter-frequency biases:\nreceive(\n    (l1_channel, l5_channel),\n    ((GPSL1CA(),), (CombinedSignal(GPSL5Q(), GPSL5I()),)),\n    sampling_freq;\n    interm_freqs = (0.0u\"Hz\", 0.0u\"Hz\"),\n)\n\nA CombinedSignal(pilot, data) tracks the dataless pilot (which the loops range on) and the data component (whose navigation message is decoded) together in one group.","title":"Systems: multi-constellation and multi-band"},{"category":"section","location":"parameters/#Acquisition","page":"Acquisition & Tracking Parameters","text":"Acquisition is the search that finds which satellites are visible and gives a first estimate of their code phase and Doppler. It re-runs at most every acquire_every of signal time. Its coherent integration length and Doppler resolution are no longer user knobs — they are derived internally, per system, from the tracking loop's carrier-Doppler pull-in range, so that the worst-case post-acquisition residual always lands inside the loop's capture range. A detection is accepted purely on a CFAR test (constant false-alarm rate) at a fixed internal false-alarm probability — there is no acquisition CN0 threshold.\n\nKeyword Default Meaning\nacquire_every 10u\"s\" How often (in signal time) acquisition re-runs to look for new satellites.\nprns nothing Which PRNs to search for. nothing ⇒ each constellation's default range; a per-GNSS NamedTuple/Dict keyed by constellation; or a plain collection applied to every system.\nacquire_async false Run each band's search on its own task instead of inline (live receivers only — see below).\nprocessing_threadpool :default (:interactive for the hardware-correlator method) Thread pool of the chunk-processing task — see below.","title":"Acquisition"},{"category":"section","location":"parameters/#Where-the-search-runs-(acquire_async)","page":"Acquisition & Tracking Parameters","text":"One scan is a full grid search: milliseconds on a workstation, but seconds on an embedded host at a few MS/s. By default it runs inline, on the same task that tracks every chunk — so for its whole duration no chunk is processed.\n\nReplaying a file that costs nothing (the file waits), and it keeps a run reproducible. On a live receiver it is a real-time overrun: the stream buffers up and the pipeline then catches up by processing stale chunks back to back. With acquire_async = true the chunk pipeline instead hands a copy of the window to a worker task and keeps tracking; the results are merged a few chunks later, with each satellite's code phase advanced from the window to the chunk that merges it.\n\nUse it for live streams and leave it off for replay. It needs at least two threads (julia -t auto) to overlap the search with tracking at all, and it is on by default for the hardware-correlator method (receive(::AbstractHardwareCorrelatorSDR, …)), where a stalled pipeline also delays the NCO feedback past the sample indices it was scheduled for and costs every lock. Measured on a LiteX-M2SDR at 4 MS/s with a 30 s rescan cadence: the receiver stayed within 0.7 s of real time across ten background scans, where the same run with an inline search lost every lock at each scan (a ~13 s pipeline stall).\n\nA background scan still shares the machine with the chunk pipeline. acquire! spawns one chunk task per default-pool thread, and each runs its share of the PRNs through the whole scan without yielding — on a Jetson Orin at 4 MS/s with 10 ms coherent and five noncoherent rounds, about 1.6 s. Julia's scheduler is cooperative, so a processing task that lives on that pool and yields (every chunk does, on take!) resumes only when one of those chunk tasks finishes. For the software receiver that is a late chunk and nothing more. For a hardware correlator it is a loop transient: the device holds each channel's last NCO word, a correction sized for a one- or two-millisecond epoch, for the whole stall, and the carrier phase slews by hundreds of degrees before the next update lands — measured on sky as navigation-word parity failures that appear only in words whose fold saw a stalled chunk (issue #107). The hardware method therefore runs the processing task on the interactive pool by default (processing_threadpool = :interactive), which the scan's chunk tasks never enter. Start Julia with -t N,M so that pool exists (with no interactive threads Julia runs such tasks on the default pool, and the setting does nothing), and count its occupants when choosing M: Julia places the main thread in the interactive pool, so the main task and anything it compiles live there too, and a vendor package's device-service tasks typically keep a thread each. The LiteX-M2SDR example needs -t N,4.","title":"Where the search runs (acquire_async)"},{"category":"section","location":"parameters/#Restricting-the-PRN-search","page":"Acquisition & Tracking Parameters","text":"prns narrows (or widens) the satellites acquisition searches for. For a multi-GNSS run you can give a per-constellation list keyed by constellation name:\n\ndata_channel = receive(\n    measurement_channel, (GPSL1CA(), GalileoE1B()), sampling_freq;\n    prns = (GPS = [1, 8, 30], Galileo = [3, 9, 24]),\n)\n\nA plain collection is applied to every system, and nothing uses each constellation's default range — GPS 1:32, Galileo 1:36, BeiDou 1:63. Each system's search is further restricted to the PRNs that actually broadcast its signal (GPS L5/L2C to the blocks that carry them; BeiDou B1C/B2a/B2b to the BDS-3 MEO/IGSO range 19-50).","title":"Restricting the PRN search"},{"category":"section","location":"parameters/#Front-end-and-correlator","page":"Acquisition & Tracking Parameters","text":"Keyword Default Meaning\nnum_ants NumAnts(1) Number of antenna channels. Must match the columns of the measurement channel(s).\ninterm_freq / interm_freqs 0.0u\"Hz\" Intermediate frequency of the incoming samples (single-band interm_freq; a tuple interm_freqs for the multi-band method).\ndownconvert_and_correlator auto (by element type) The correlator backend. nothing auto-selects: Tracking's fast integer backend for Complex{Int16} samples, the float CPUThreadedDownconvertAndCorrelator() otherwise. Pass one explicitly to override.\nmax_meas nothing Front-end full-scale (largest |real|/|imag| of any sample, e.g. 2^11 for a 12-bit ADC). Required for Complex{Int16} samples (the integer backend); ignored for float samples or when downconvert_and_correlator is given.\n\nThe correlator backend is auto-selected from the sample element type: Complex{Int16} recordings use Tracking's fast integer downconvert-and-correlator (which needs max_meas), and every other element type uses the float CPUThreadedDownconvertAndCorrelator(). You can therefore either pass Complex{Int16} samples with max_meas, or read integer recordings as ComplexF32 (read_uint8_iq_file(...; center = 127.5, type = ComplexF32)) to use the float backend with no full-scale value. See Getting Started.","title":"Front end & correlator"},{"category":"section","location":"parameters/#Lock-detection","page":"Acquisition & Tracking Parameters","text":"A satellite contributes to the PVT solution only while it is in lock. Lock is declared per satellite by a CodeLockDetector — a threshold on the post-integration SNR — and a CarrierLockDetector — the narrowband difference/power phase-lock indicator Σ(I²−Q²)/Σ(I²+Q²), fed every prompt the tracking loops produce. Both track elapsed signal time, so their behaviour is independent of how the signal is chunked, and both take their timings as counts of the ranging signal's primary code period rather than in seconds, so they mean the same thing on every signal (see Timings scale with the code period).\n\nThe detector thresholds and timings (the code-lock CN0 threshold, the carrier phase-lock threshold and its smoothing window, and the out-of-lock and warm-up windows) are set at detector construction from per-signal defaults; see their docstrings in the API Reference.\n\nThe CN0 threshold is referred to a record spanning one primary code period, because what decides detectability is the post-integration SNR CN0 · T rather than CN0 on its own. When tracking integrates over N code blocks per record, a satellite therefore holds lock at 10·log10(N) dB less CN0 — 3 dB at two blocks — since the longer coherent integration recovers exactly that much. Anchoring to the code period rather than to the symbol period keeps the number meaning the same thing for the default one-block cadence and stays defined for pilot signals, which have no data rate.\n\nThe CN0 itself is estimated by Tracking, whose default estimator is the measured noise reference (NoiseRefCN0Estimator) as of Tracking 7: it divides each record's prompt power by a noise density measured by despreading the signal's own code at a randomised phase, through the same correlator the satellites go through, rather than inferring a floor from the prompt stream. The receiver provisions one such reference per tracked signal automatically, so nothing has to be configured for it.\n\nThe threshold keeps its units and its meaning, but the statistic it is compared against is a better behaved one. It has no noise-only floor to clear — on pure noise the estimate is -Inf dBHz or single digits, against the moment ratio's ~27.6 dB-Hz — so a dead or falsely acquired channel drops out of lock promptly rather than clearing a 30 dB-Hz threshold on ~19 % of records. And because the floor is measured rather than inferred, the number is the true CN0: on the ION reference recording it reads a median 1.0 dB above what the narrowband/wideband ratio (Tracking 6's default) reported, and up to 2.4 dB on the least steadily tracked satellite, whose loop phase noise that coherent estimator charged to the signal. The threshold is not a receive keyword: it comes from the per-signal default (GNSSReceiver.get_default_code_lock_cn0_threshold, 30 dB-Hz) that each satellite's CodeLockDetector is constructed with, which is where sensitivity would have to be traded. The estimate is also what sat_data[…].cn0 reports and what the GUI plots.","title":"Lock detection"},{"category":"section","location":"parameters/#Two-stages:-handover,-then-steady-state","page":"Acquisition & Tracking Parameters","text":"The acquisition → tracking handover is deliberately coarse — the acquisition Doppler bin is half the tracking loop's pull-in range, and the code phase is only resolved to the sample grid, halved by subsample_interpolation. Until the loops have pulled those errors in, the prompt correlator sits off the correlation peak and reports less power than the satellite really has, and the carrier phase is still spinning at the residual Doppler, so a perfectly healthy satellite looks weak to both detectors.\n\nBoth detectors therefore run a pull-in stage with a generous out-of-lock allowance, latching permanently into a tighter steady state once the satellite has either strung together enough good evidence to count as converged or exhausted its pull-in window. This buys handover tolerance without making a genuine signal loss slower to notice. Measured on synthetic handovers at this receiver's own worst-case residual, the peak accumulated out-of-lock time a satellite the receiver should keep demands is 292 code periods — more than the 200-code-period steady-state dwell tolerates, which is why a single un-staged dwell drops it.\n\nRanging is gated separately from tracking, and on a later criterion. A satellite in its pull-in stage is still kept — that is the point — but it does not contribute to the PVT solution until its loops have settled, because a code phase that is still converging produces a pseudorange biased by tens of metres: measured at a 0.25-chip handover, the residual is 0.15 to 0.22 chip (44 to 64 m) after 200 code periods of clean evidence and 0.018 to 0.15 chip (5 to 44 m) after 600. time_in_lock_before_calculating_pvt alone would not achieve this: it counts from the handover, so it can elapse while the loops are still settling — and on a code longer than 1 ms the settling takes proportionally longer while that gate stays fixed in seconds.\n\nRanging readiness needs a timer as well as an evidence path, for the same reason the pull-in stage does: the evidence clock is reset by any out-of-lock verdict, so an uninterrupted run is a bar a satellite can fail to clear indefinitely — and it would then be tracked, converged, and silently absent from the PVT solve forever. Since PVT needs four satellites, that trades a degraded fix for no fix. Under Tracking's default noise-reference estimator the code detector rarely blocks the run (measured steady-state per-chunk out-of-lock rates are 0.58 at the 30 dB-Hz threshold itself, 0.10 one dB above it and 0.00 from two dB up), but the carrier detector does: its phase-lock indicator takes 460 to 590 code periods to come up through the handover. And against any disturbance recurring on a period shorter than the run the evidence path never fires at all, at any C/N0. The backstop admits such a satellite once enough signal has elapsed — two code-loop time constants — with its out-of-lock clock paid down to zero. What it shortens is the length of the clean run it asks for, not the requirement that the channel be clean: a channel failing steadily enough never to pay its debt off is dropped by the dwell rather than admitted here. What it concedes is a few metres of extra code-phase thermal jitter (1σ of 0.039 chip, 11 m, at 30 dB-Hz against 0.004 chip, 1.2 m, at 45), not a converging handover bias.\n\nOne residual case is left open deliberately: a satellite whose accumulated out-of-lock time parks between the steady-state and pull-in allowances — 200 to 600 code periods — never leaves the pull-in stage, and so never becomes ranging-ready either. Reaching it takes a burst of failures followed by a sustained near-50% duty cycle; under a stochastic signal the clock random-walks out of that band, either down to zero (whereupon it latches) or up past the pull-in allowance (whereupon lock is lost and the satellite is reacquired). Closing it would mean letting the pull-in stage latch while its clock is still above the steady-state threshold, which is exactly the discontinuity the latch guard exists to prevent.","title":"Two stages: handover, then steady state"},{"category":"section","location":"parameters/#Timings-scale-with-the-code-period","page":"Acquisition & Tracking Parameters","text":"Every detector timing is configured as a multiple of the ranging signal's primary code period rather than in seconds, because that is what the tracking loops' time constants scale with: Tracking sizes its default bandwidths at B_L·T ≈ 0.018, so a 1 ms code gets an 18 Hz carrier / 1 Hz code loop while a 10 ms code gets 1.8 Hz / 0.1 Hz — every 1/B_L is a fixed number of code periods.\n\nSignal Code period Warm-up (80 T) Code dwell (200 T) Pull-in allowance (600 T) Ranging ready (≥ 680 T) Ranging backstop (2000 T)\nGPS L1 C/A 1 ms 80 ms 200 ms 600 ms ≥ 680 ms 2 s\nGalileo E1B 4 ms 320 ms 800 ms 2.4 s ≥ 2.7 s 8 s\nGPS L1C-D 10 ms 800 ms 2 s 6 s ≥ 6.8 s 20 s\n\n\"Ranging ready\" is the evidence path — the warm-up plus an uninterrupted 600-code-period run, so 680 T is a floor rather than a typical value: measured on real handovers it lands at 760 to 1500 code periods on GPS L1 C/A, because the carrier loop has its own Doppler pull-in to do before the phase-lock indicator stops resetting the run. \"Ranging backstop\" is the timer that bounds the wait for a satellite whose evidence never comes cleanly. The carrier detector's own dwell is 4000 T (4 s on GPS L1 C/A) and, being floored at that, is unstaged: it clears outright on any favourable chunk, so declaring loss already needs a consecutive run of failures far longer than any handover transient.\n\nA dwell fixed in seconds means something different on every signal: the 200 ms this receiver used to allow is 200 code periods on GPS L1 C/A — the signal it was tuned against, and where it is correct — but only 50 on Galileo E1B and 20 on GPS L1C-D, against loops that are four and ten times slower. That is why lock detection failed on the slower signals while GPS L1 C/A looked fine. The GPS L1 C/A warm-up and dwell are unchanged.\n\nOne consequence worth stating outright: on codes longer than 1 ms these timings, not time_in_lock_before_calculating_pvt, are what actually decide when a satellite joins the solve. A healthy GPS L1 C/A satellite is ranging-ready between 0.76 s and 1.5 s depending on its C/N0, so the 2 s default still binds there. On Galileo E1B (2.7 s at best) and GPS L1C-D (6.8 s) it no longer does, and lowering it below those figures has no effect at all. The extra seconds are in practice dwarfed by the tens of seconds of subframe decoding that set time to first fix.\n\nThe counts themselves are set at detector construction; see the CodeLockDetector and CarrierLockDetector docstrings in the API Reference for the defaults.","title":"Timings scale with the code period"},{"category":"section","location":"parameters/#PVT","page":"Acquisition & Tracking Parameters","text":"Keyword Default Meaning\ntime_in_lock_before_calculating_pvt 2u\"s\" A satellite must be locked this long before it is used for PVT.\npvt_update_interval 100u\"ms\" How often the PVT solution is recomputed (also the rate at which the data channel emits).\nenable_ionospheric_correction true Apply the broadcast ionospheric correction.\nenable_tropospheric_correction true Apply the tropospheric correction.\npvt_approximate_year current UTC year Resolves the GPS week-number rollover for old recordings.\n\npvt_approximate_year matters for archived data: an old recording processed with the wrong year lands ~19.6 years off. The Worked Example (Real Data) sets pvt_approximate_year = 2017 for its 2017 recording.\n\ndata_channel = receive(\n    measurement_channel, GPSL1CA(), 2.048e6u\"Hz\";\n    pvt_update_interval = 200u\"ms\",\n    enable_tropospheric_correction = false,\n    pvt_approximate_year = 2017,\n)","title":"PVT"},{"category":"section","location":"parameters/#Vector-tracking","page":"Acquisition & Tracking Parameters","text":"By default every satellite closes its own code/carrier loops (scalar tracking). vector_tracking = true switches the receiver to vector tracking: once a first scalar fix is available, a navigation Kalman filter fuses all satellites' accumulated discriminator outputs into one position/velocity/clock solution and closes every tracking loop centrally from it (a vector delay/frequency lock loop, VDFLL). Weak or briefly obscured satellites are carried through outages by the collective solution, and the emitted PVT solutions come from the navigation filter (at pvt_update_interval). They carry the same per-satellite diagnostics as a scalar fix, including the post-fit pseudorange and range-rate residuals (SatInfo.residual and SatInfo.rate_residual) of every satellite in the vector loop — reported even for a satellite whose signal the loop is currently carrying through an outage.\n\ndata_channel = receive(\n    measurement_channel, (GPSL1CA(), GalileoE1B()), 2.048e6u\"Hz\";\n    vector_tracking = true,\n)\n\nMulti-constellation and multi-band configurations are supported with the same bias model as the scalar solve: the filter estimates one receiver clock bias per GNSS time system (all driven by the one oscillator's clock drift) and one receiver inter-frequency bias per band beyond a reference band. The pseudorange measurements are corrected for the broadcast ionospheric model and the Saastamoinen tropospheric delay, exactly like calc_pvt (toggled by the same enable_ionospheric_correction / enable_tropospheric_correction keywords).\n\nvector_tracking = true runs the filter with its default configuration. To configure it, pass a VectorTracking instead — it both enables vector tracking and describes the platform and the front end, which is worth doing whenever either is known: the defaults assume vehicular dynamics and a TCXO-grade oscillator, and both the dynamics (acceleration_noise_std) and the oscillator stability (h0, hm2) materially change how the filter weighs its prediction against the measurements. The same struct also selects the measurement set (use_pseudorange_rates = false for a pseudorange-only VDLL instead of the default VDFLL), the motion and clock model orders, the inter-frequency-bias process noise and how long the filter may coast before falling back to scalar tracking:\n\ndata_channel = receive(\n    measurement_channel, (GPSL1CA(), GalileoE1B()), 2.048e6u\"Hz\";\n    vector_tracking = VectorTracking(;\n        acceleration_noise_std = 1.0u\"m/s^2\",   # pedestrian dynamics\n        insufficient_meas_timeout = 30.0u\"s\",\n    ),\n)\n\nEach emitted payload then reports what the filter is doing through its VTStatus: whether it is running, its own position and clock uncertainty, every loop member's post-fit residuals (coasted members included) and how long it has been coasting on epochs it could not solve.","title":"Vector tracking"},{"category":"section","location":"parameters/#Custom-per-chunk-output","page":"Acquisition & Tracking Parameters","text":"extract replaces the per-chunk payload builder; the default default_data_of_interest emits a ReceiverDataOfInterest. Pass your own extract(receiver_state) to emit anything else — see Custom Receiver Output.","title":"Custom per-chunk output"},{"category":"section","location":"parameters/#Multi-antenna-processing","page":"Acquisition & Tracking Parameters","text":"For multiple antennas (NumAnts(N) with N > 1) the post-correlation filter is an EigenBeamformer. The number of antenna channels in each measurement channel must equal N in num_ants.\n\nThe reported C/N₀ is referenced to the beamformer's own output: Tracking measures the array's N×N spatial noise covariance R̂ once per signal and each satellite reduces it through its current weights, N₀ = wᴴR̂w. So the array gain a well-steered beam buys shows up in C/N₀, and only that — a beamformer's weight scaling cancels out of the ratio.","title":"Multi-antenna processing"},{"category":"section","location":"parameters/#Full-reference","page":"Acquisition & Tracking Parameters","text":"The complete, authoritative list of keyword arguments — with their exact defaults — is in the docstrings of receive and ReceiverState in the API Reference.","title":"Full reference"},{"category":"section","location":"signal_support/#Signal-Support-and-Evidence","page":"Signal Support & Evidence","text":"GNSSReceiver's constructors accept every signal GNSSSignals defines. That is not the same as every signal being validated, and this page is the difference: a per-signal, per-role record of what has actually been demonstrated, how, and what the rest is waiting on.\n\nIt exists because \"supported\" is not a property a receiver can claim of a signal as a whole. A pilot never decodes a navigation message; a data component is rarely what the loops range on; a signal whose replica is provably correct may still have no tracking baseline behind it. So support is recorded per (signal, role) pair, and a cell that has no evidence says so rather than inheriting a claim from its neighbours.\n\nThe table below is generated from test/signal_support.jl and checked against it on every test run, so it cannot drift from what the suite actually asserts. The checks that fill it in live in test/signal_validation.jl and run against the deterministic reference harness described at the end of this page.\n\nnote: Most cells are `untested`, on purpose\nThis matrix is being filled in by the all-signal roadmap (issue #130). The hardware-correlator steps have landed and the first live-RF cells are in; the per-signal sweeps have not been run. An untested cell means no evidence exists, not that the capability is known to be missing — and recording that honestly is the entire value of the artifact. A cell only turns into software, simulated_fpga, hardware_replay or live_rf when a check produces that evidence.\n\nnote: Simultaneous multi-band reception is not a cell in this table\nSupport is recorded per (signal, role) because those are properties of the signal. Whether a given receiver can have two of these signals in the air at the same time is a property of its front end — how many bands it can tune at once, on which inputs, at which sample rates, and on what clock — and it is declared, validated and documented in the hardware-correlator contract instead (section 5a). A row that reads supported for every role still says nothing about receiving that signal alongside one from another band.","title":"Signal Support & Evidence"},{"category":"section","location":"signal_support/#Evidence-levels","page":"Signal Support & Evidence","text":"Level What it means\nsoftware The software receive path, over reference-harness samples or a recording.\nsimulated_fpga The simulated hardware correlator (test/simulated_fpga.jl), fed the same samples.\nhardware_replay Recorded samples replayed through real gateware.\nlive_rf A live antenna through the hardware-correlator path.\n\nsoftware evidence covers every row; simulated_fpga evidence exists for the GPS L5I overlay removal; and live_rf evidence exists for Galileo E1B with the BOC(1,1) replica, the first non-GPS signal tracked through the hardware correlator on sky (2026-09-18, four-channel five-tap LiteX-M2SDR gateware; the record is examples/analysis/hardware_live_m2sdr.md). A live-RF cell names a field record rather than a test: the run cannot be repeated without the board, so the matrix says where the log is instead of pretending CI produced it. The harness is deliberately arranged so every path compares against the same reference rather than against each other.","title":"Evidence levels"},{"category":"section","location":"signal_support/#The-matrix","page":"Signal Support & Evidence","text":"<!– BEGIN GENERATED MATRIX –> | Family | Signal | replica | acquisition handover | tracking | secondary sync | data decode | pvt | |—|—|—|—|—|—|—|—| | GPS L1 C/A | GPSL1CA | software (harnessreplica) | software (harnessacquisition) | software (harnessreceive) | n/a (nosecondarycode) | software (ionrecording) | software (ionrecording) | | GPS L1C | `GPSL1CD| software (harness_replica) | software (harness_acquisition) | untested (tracking_sweep_pending) | n/a (no_secondary_code) | untested (decode_sweep_pending) | untested (pvt_sweep_pending) | |  |GPSL1CP` | software (harnessreplica) | software (harnessacquisition) | untested (trackingsweeppending) | untested (secondarysyncsweeppending) | n/a (pilotnodata) | untested (pvtsweeppending) | | GPS L2C | GPSL2CM | software (harnessreplica) | software (harnessacquisition) | untested (trackingsweeppending) | n/a (nosecondarycode) | untested (decodesweeppending) | untested (pvtsweeppending) | |  | GPSL2CL | software (harnessreplica) | n/a (acquisitionwindowtoolong) | untested (l2cltrackingsweeppending) | n/a (nosecondarycode) | n/a (pilotnodata) | untested (pvtsweeppending) | | GPS L5 | GPSL5I | software (harnessreplica) | software (harnessacquisition) | untested (trackingsweeppending) | untested (secondarysyncloopprocesspending) | untested (decodesweeppending) | untested (pvtsweeppending) | |  | GPSL5Q | software (harnessreplica) | software (harnessacquisition) | untested (trackingsweeppending) | untested (secondarysyncsweeppending) | n/a (pilotnodata) | untested (pvtsweeppending) | | Galileo E1 | GalileoE1B | software (harnessreplica) | software (harnessacquisition) | untested (trackingsweeppending) | n/a (nosecondarycode) | untested (decodesweeppending) | untested (pvtsweeppending) | |  | GalileoE1C | software (harnessreplica) | software (harnessacquisition) | untested (trackingsweeppending) | untested (secondarysyncsweeppending) | n/a (pilotnodata) | untested (pvtsweeppending) | |  | `GalileoE1BBOC11| software (harness_replica) | software (harness_acquisition) | live_rf (live_m2sdr_l1_20260918) | n/a (no_secondary_code) | live_rf (live_m2sdr_l1_20260918) | live_rf (live_m2sdr_l1_20260918) | |  |GalileoE1CBOC11` | software (harnessreplica) | software (harnessacquisition) | untested (trackingsweeppending) | untested (secondarysyncsweeppending) | n/a (pilotnodata) | untested (pvtsweeppending) | | Galileo E5 | GalileoE5aI | software (harnessreplica) | software (harnessacquisition) | untested (trackingsweeppending) | untested (secondarysyncsweeppending) | untested (decodesweeppending) | untested (pvtsweeppending) | |  | GalileoE5aQ | software (harnessreplica) | software (harnessacquisition) | untested (trackingsweeppending) | untested (secondarysyncsweeppending) | n/a (pilotnodata) | untested (pvtsweeppending) | |  | GalileoE5aQP | software (harnessreplica) | software (harnessacquisition) | untested (trackingsweeppending) | n/a (nosecondarycode) | n/a (pilotnodata) | untested (pvtsweeppending) | |  | GalileoE5bI | software (harnessreplica) | software (harnessacquisition) | untested (trackingsweeppending) | untested (secondarysyncsweeppending) | untested (decodesweeppending) | untested (pvtsweeppending) | |  | GalileoE5bQ | software (harnessreplica) | software (harnessacquisition) | untested (trackingsweeppending) | untested (secondarysyncsweeppending) | n/a (pilotnodata) | untested (pvtsweeppending) | | Galileo E6 | GalileoE6B | software (harnessreplica) | software (harnessacquisition) | untested (trackingsweeppending) | n/a (nosecondarycode) | untested (decodesweeppending) | untested (pvtsweeppending) | |  | GalileoE6C | software (harnessreplica) | software (harnessacquisition) | untested (trackingsweeppending) | untested (secondarysyncsweeppending) | n/a (pilotnodata) | untested (pvtsweeppending) | | BeiDou legacy/B2 | BeiDouB1I | software (harnessreplica) | software (harnessacquisition) | untested (trackingsweeppending) | untested (secondarysyncsweeppending) | untested (decodesweeppending) | untested (pvtsweeppending) | |  | BeiDouB3I | software (harnessreplica) | software (harnessacquisition) | untested (trackingsweeppending) | untested (secondarysyncsweeppending) | untested (decodesweeppending) | untested (pvtsweeppending) | |  | BeiDouB2bI | software (harnessreplica) | software (harnessacquisition) | untested (trackingsweeppending) | n/a (nosecondarycode) | untested (decodesweeppending) | untested (pvtsweeppending) | |  | BeiDouB2aI | software (harnessreplica) | software (harnessacquisition) | untested (trackingsweeppending) | untested (secondarysyncsweeppending) | untested (decodesweeppending) | untested (pvtsweeppending) | |  | BeiDouB2aQ | software (harnessreplica) | software (harnessacquisition) | untested (trackingsweeppending) | untested (secondarysyncsweeppending) | n/a (pilotnodata) | untested (pvtsweeppending) | | BeiDou B1C | `BeiDouB1CD| software (harness_replica) | software (harness_acquisition) | untested (tracking_sweep_pending) | n/a (no_secondary_code) | untested (decode_sweep_pending) | untested (pvt_sweep_pending) | |  |BeiDouB1CP` | software (harnessreplica) | software (harnessacquisition) | untested (trackingsweeppending) | untested (secondarysyncsweeppending) | n/a (pilotnodata) | untested (pvtsweeppending) |","title":"The matrix"},{"category":"section","location":"signal_support/#Roles","page":"Signal Support & Evidence","text":"replica — The code replica is generated correctly: unit-power, balanced, correlating to a single dominant peak at the stated code phase, isolated from other PRNs, and (where there is one) carrying the right secondary code.\nacquisition handover — A cold acquisition detects the satellite and hands over a code phase and Doppler accurate enough for the tracking loops to pull in.\ntracking — The code and carrier loops hold lock and keep the replica aligned.\nsecondary sync — The receiver synchronises to the secondary (overlay) code, so integration can extend past one primary code period.\ndata decode — The navigation message is demodulated and decoded into a usable ephemeris.\npvt — The satellite contributes valid measurements to a position/velocity/time fix.","title":"Roles"},{"category":"section","location":"signal_support/#Evidence-sources","page":"Signal Support & Evidence","text":"harness_acquisition (test/signal_validation.jl) — The reference harness's per-signal acquisition check: a cold acquire! over harness samples has to detect the satellite and hand over a code phase and Doppler within the stated tolerances.\nharness_receive (test/signal_validation.jl) — A full receive run over harness samples: the satellite has to be acquired, tracked and held in lock with its C/N₀ within tolerance of the case's.\nharness_replica (test/signal_validation.jl) — The reference harness's per-signal replica check: unit code power, code balance, peak alignment against a fractional code phase and a Doppler, main-peak dominance, cross-PRN isolation and secondary-code agreement, all against ReferenceHarness' noise-free reference.\nion_recording (test/ion_rtlsdr_integration.jl) — The 60 s ION RTL-SDR live-sky GPS L1 recording through the software receive path, asserted against a captured baseline: eleven healthy satellites, decoded ephemerides, and a PVT fix repeatable to one metre per ECEF component.\nlive_m2sdr_l1_20260918 (examples/analysis/hardware_live_m2sdr.md) — Live sky on orin2 through the LiteX-M2SDR hardware correlator, 2026-09-18, gateware gnss_m2sdr_m2_x1_ch4_ant1_code4092_tap5_sub12_placeSpread (four channels, five taps, 4092-chip code memory, sub-chip replicas), fs = 4 MS/s, one antenna seeing about a quarter of the hemisphere, run by the in-process adapter's live example (retired with it; see the record file and git history). GPS L1 C/A on three-tap channels held PRN 14 at 50–52 dBHz for 300 s and, on the six-channel build with the record sizing fixed, decoded four LNAV ephemerides and produced a GPS fix after 205 s; Galileo E1B (BOC(1,1) replica on five-tap channels) held four satellites at 36–47 dBHz, decoded their I/NAV ephemerides and produced a Galileo-only position fix after 40 s. The log excerpts and the counters are in the record file; the run is not reproducible without the board.","title":"Evidence sources"},{"category":"section","location":"signal_support/#Why-a-cell-is-not-supported","page":"Signal Support & Evidence","text":"acquisition_window_too_long — One coherent acquisition window is a whole primary code period, and this signal's is 1.5 s — 3 million samples at four samples per chip. So L2CL is never the signal a receiver acquires: acquisition_signal falls back to the pairing's L2CM data component above ~0.67 Hz Doppler resolution, and L2CL is handed over from L2CM's code phase rather than searched for. A property of the signal, not a gap in the evidence.\ndecode_sweep_pending — A decoder exists for this signal (GNSSDecoderState has a method), but nothing has demonstrated a decode. Synthetic samples cannot: the harness modulates a reproducible bit stream, not a navigation message with a preamble, parity and an ephemeris. Evidence has to come from a recording or live sky.\nl2cl_tracking_sweep_pending — The hardware path's record accounting for a 1.5 s primary code (records cut inside the code period, no short record counted as a completed period) was validated on the retired in-process link and is carried by the loop core (HardwareLoopCore.jl); nothing has yet run L2CL through the loop process. That is not a tracking sweep either way: nothing has shown the carrier loop holding lock on L2CL — on the in-process run its Doppler estimate barely moved against a deliberate offset while GPS L1 C/A through the identical harness converged — and whether that is a property of the signal, of the 20 ms coherent window a 1.5 s code forces, or of Tracking's per-signal support is the subject of the software-support audit (JuliaGNSS/Tracking.jl#236) and the per-signal tracking sweep of step 9.\nno_secondary_code — The signal has no secondary code (get_secondary_code_length is 1), so there is nothing to synchronise to.\npilot_no_data — A pilot component carries no navigation data (get_data_frequency is 0 Hz), so there is no message to decode. It contributes to a fix through its CombinedSignal pairing with the data component, not on its own.\npvt_sweep_pending — No fix has been computed with this signal contributing. Follows the decode sweep for a data component, and the tracking sweep for a pilot, which contributes pseudoranges through its CombinedSignal pairing.\nsecondary_sync_loop_process_pending — The overlay removal after synchronisation (issue #132) was demonstrated end to end for GPS L5I on the retired in-process link (the device replicated the primary code only, Tracking's detector found the NH10 overlay, the link removed it from every record, and the decoded symbols were the harness's). The loop core carries the same ingest path, but nothing has yet run a secondary-code signal through the loop process: that demonstration is the follow-up.\nsecondary_sync_sweep_pending — The receiver's own secondary-code synchronisation is not swept per signal yet. The harness does verify that the secondary code is recoverable from the reference (it is part of the replica check), which is the prerequisite, not the capability.\ntracking_sweep_pending — No per-signal tracking sweep exists yet. The harness can generate the case; what is missing is the run and its baseline — part of step 9 itself, and only meaningful once the loops it exercises are the ones the roadmap settles on.\n\n<!– END GENERATED MATRIX –>","title":"Why a cell is not supported"},{"category":"section","location":"signal_support/#The-reference-harness","page":"Signal Support & Evidence","text":"Every software cell above is produced by one generator and compared against one reference model, both in test/reference_harness.jl. The point is that a later step cannot invent its own: \"the software path and the gateware agree\" only means something when both were handed the same samples and both were measured against the same reference with the same stated tolerance.\n\nA case is described entirely by its parameters — signal variant, PRN, sampling frequency, intermediate frequency, Doppler, fractional code phase, carrier phase, C/N₀ or amplitude, noise power, antenna count and steering vector, optional data modulation, and a seed:\n\ncase = ReferenceCase(\n    GalileoE1B();\n    prn = 11,\n    sampling_freq = 24.552e6,\n    carrier_doppler = 1234.0,\n    code_phase = 1513.4271,   # fractional chips\n    cn0_dbhz = 45.0,\n    num_ants = 2,\n    steering = [1.0, 0.5im],\n)\nsamples = generate_samples(case, 40_000)\n\nTwo properties make it usable as a shared reference:\n\nDeterminism. The same case produces bit-identical samples on any machine.\nChunk independence. The noise is drawn one sample at a time from a single seeded generator and the signal is a closed-form function of the absolute sample index, so reading the stream in 4000-sample chunks and reading it in one block give the same samples. A device replay and a direct correlation therefore see one signal.\n\nreference_correlation(case, layout, num_samples) is the reference itself: what an ideal correlator locked on the truth would accumulate, for a three-tap (epl_taps()) or five-tap (vepl_taps()) layout, per antenna. Every path under test is compared against it, never against another path, so a bug shared by two implementations cannot pass as agreement.","title":"The reference harness"},{"category":"section","location":"signal_support/#Tolerances","page":"Signal Support & Evidence","text":"Comparisons report named deviations against an explicit budget rather than a bare verdict, so a failure says which quantity moved:\n\nQuantity Default budget Why\ncode_phase_chips 0.01 chips ~3 m on GPS L1 C/A, far below any loop's own jitter.\ncarrier_phase_cycles 0.02 cycles ≈ 7°.\ndoppler_hz 1 Hz Below one acquisition bin at any coherent length the receiver uses.\namplitude_relative 5 % The replica-amplitude budget: a differently scaled replica lands here rather than silently in the code-phase number.\ncn0_db 1 dB \ncorrelation_relative 5 % Per-tap complex agreement, normalised by the reference prompt.\ncross_correlation_relative 15 % The ceiling a wrong PRN must stay under; the worst Gold-code cross-correlation is only ~24 dB down.\n\nTwo derived budgets keep those numbers honest rather than aspirational:\n\nquantization_tolerances(num_bits) widens the amplitude-related entries by a num_bits quantiser's own loss, so a comparison against a quantised path states its quantisation instead of assuming it away.\nstatistical_tolerances(case, num_samples) widens them by five standard deviations of the thermal noise the case itself specifies. A 45 dBHz signal integrated over one GPS L1 C/A code period carries 18 % accumulator noise, so no comparison at that C/N₀ and that window can hold 5 % — lengthening the window is what buys a tighter budget, and the budget says so.\n\nThe code-phase budget additionally carries code_phase_resolution(case), half a sample in chips. A sampled code is piecewise constant, so the correlation peak is a plateau one sample wide rather than a point; no code-phase estimate can be read more sharply than that, however clean the signal.","title":"Tolerances"},{"category":"section","location":"signal_support/#Known-limitations-of-the-software-evidence","page":"Signal Support & Evidence","text":"The generator models an infinite-bandwidth front end. It evaluates the ideal code at the sampling instants; a real front end band-limits first, which rounds the chip transitions off and costs correlation power. This does not affect comparisons between the software path, a simulated gateware and a replay of these same samples — all three see the same stream — but absolute amplitudes against live RF will differ.\nBOC signals need a fast sampler. A BOC(m, n) sub-carrier flips 2m/n times per chip, and GNSSSignals' code generator refuses to produce a replica below one sample per half-cycle. The BOC(6, 1) component of Galileo E1B/E1C and GPS L1C-P puts that floor at 12.276 MHz — which is why those rows are acquired at 24.55 MHz while GPS L1 C/A manages at 4.092 MHz.\nSynthetic samples carry no navigation message. The harness can modulate a reproducible ±1 bit stream, which is enough to make a tracking loop face bit transitions, but it has no preamble, parity or ephemeris. Every data_decode and pvt claim therefore has to come from a recording or live sky, which is why GPS L1 C/A is the only row with one.\nSimultaneous all-band reception is a separate constraint. A single sample stream carries one RF band, and the matrix says nothing about how many bands a given front end can receive at once.","title":"Known limitations of the software evidence"},{"category":"section","location":"signal_support/#Reproducing-it","page":"Signal Support & Evidence","text":"The whole matrix is produced by the test suite:\n\njulia --project=. -e 'using Pkg; Pkg.test()'\n\nTo run only the signal-validation part while iterating:\n\njulia --project=. -e 'using Test, GNSSReceiver, GNSSSignals, Acquisition, Unitful;\n                      using Unitful: Hz, ms; include(\"test/signal_validation.jl\")'\n\nAfter changing a cell in test/signal_support.jl, regenerate the block between the generated-matrix markers on this page with SignalSupport.render_markdown(); the suite fails while the two disagree.","title":"Reproducing it"},{"category":"section","location":"custom_output/#Custom-Receiver-Output","page":"Custom Receiver Output","text":"By default receive emits a compact ReceiverDataOfInterest for every processed chunk: a sat_data dictionary (each satellite's CN0, prompt correlator value and health), the current pvt solution and the runtime. That covers most needs, and since pvt is the full PositionVelocityTime.PVTSolution a lot more is already reachable from it (velocity, DOP, clock drift — see Worked Example (Real Data)).\n\nWhen you need something the summary does not carry — the raw carrier Doppler, code phase or carrier phase of the tracking loops, or the decoded navigation data — pass your own extract function to receive.","title":"Custom Receiver Output"},{"category":"section","location":"custom_output/#How-it-works","page":"Custom Receiver Output","text":"receive runs the acquire → track → decode → PVT pipeline and, each processed chunk, calls extract(receiver_state) on the full ReceiverState and puts the result on the returned channel. The default is default_data_of_interest, which builds the ReceiverDataOfInterest. Because your function decides the payload, the channel's element type is inferred from what it returns (so the channel stays type-stable), and consumers like collect_data and save_data work unchanged.\n\nwarning: `extract` must be read-only and return an immutable value\nIt runs inside the tracking loop on a ReceiverState that the next chunk mutates in place (this in-place reuse is what makes the receiver allocation-free in steady state). Copy what you need into a fresh, immutable value — a named tuple, a Dict, or your own struct — and never return a reference into the state, or a later chunk will change it underneath your consumer.","title":"How it works"},{"category":"section","location":"custom_output/#What's-in-the-ReceiverState","page":"Custom Receiver Output","text":"The argument handed to extract is a ReceiverState. The fields you will usually read are:\n\nField What it holds Useful accessors\ntrack_state Tracking's per-satellite loop state, grouped by signal get_sat_states(track_state, group_key), then per sat_state: get_prn, get_carrier_doppler, get_code_phase, get_carrier_phase, estimate_cn0 (from Tracking)\nreceiver_sat_states A NamedTuple keyed by signal id (:GPSL1CA, …); each value is a Dictionaries.Dictionary{Int,ReceiverSatState} keyed by PRN GNSSReceiver.is_sat_healthy(…​.decoder), plus the decoded ephemeris in …​.decoder (from GNSSDecoder); GNSSReceiver.is_in_lock(…) and GNSSReceiver.is_ranging_ready(…) for the lock stage (see Acquisition & Tracking Parameters)\npvt Current PVT solution fields of PositionVelocityTime.PVTSolution\nruntime Elapsed signal time —\n\nThe tracking-group keys are exactly keys(receiver_sat_states) — one per constellation / signal (e.g. :GPSL1CA, :GalileoE1B). To visit every tracked satellite, iterate the group keys and, for each, the sat_states of that group:\n\nfor group_key in keys(receiver_state.receiver_sat_states)\n    for sat_state in get_sat_states(receiver_state.track_state, group_key)\n        prn = get_prn(sat_state)\n        # get_carrier_doppler(sat_state), get_code_phase(sat_state), …\n    end\nend\n\nKey any per-satellite payload by (group_key, prn) — the same key sat_data and pvt.sats use, so a satellite lines up across constellations and bands.","title":"What's in the ReceiverState"},{"category":"section","location":"custom_output/#Example","page":"Custom Receiver Output","text":"The example below emits, per satellite, the raw carrier Doppler and code phase (from the tracking loops) and the decoded health flag (from the decoder) — none of which is in the default summary. It runs on the same public recording as the Worked Example (Real Data); the download is skipped if that page already fetched it.\n\nusing Downloads, GNSSReceiver, GNSSSignals, Unitful, Tracking\n\n# Same recording as the worked example (downloaded once, then cached on disk).\nurl = \"https://sdr.ion.org/RTL_SDR/RTLSDR_Bands-L1.uint8\"\nfile = joinpath(tempdir(), \"RTLSDR_Bands-L1_prefix.uint8\")\nnbytes = 193_000_000\nif !isfile(file) || filesize(file) < nbytes\n    Downloads.download(url, file; headers = [\"Range\" => \"bytes=0-$(nbytes - 1)\"])\nend\n\nsampling_freq = 2.048e6u\"Hz\"\nsystem = GPSL1CA()\nnum_samples = Int(upreferred(sampling_freq * 4u\"ms\"))\n\nDefine an extract that copies the quantities of interest into a plain named tuple:\n\nfunction tracking_details(receiver_state)\n    track_state = receiver_state.track_state\n    receiver_sat_states = receiver_state.receiver_sat_states\n    sats = Dict(\n        (group_key, get_prn(sat_state)) => (\n            doppler = get_carrier_doppler(sat_state),\n            code_phase = get_code_phase(sat_state),\n            healthy = GNSSReceiver.is_sat_healthy(\n                receiver_sat_states[group_key][get_prn(sat_state)].decoder,\n            ),\n        )\n        for group_key in keys(receiver_sat_states)\n        for sat_state in get_sat_states(track_state, group_key)\n    )\n    return (; runtime = receiver_state.runtime, sats)\nend\ntracking_details (generic function with 1 method)\n\nPass it to receive and collect the run. Note the channel's element type is now our named tuple, not a ReceiverDataOfInterest:\n\ndata_channel = receive(\n    read_uint8_iq_file(file, num_samples; center = 127.5, type = ComplexF32),\n    system,\n    sampling_freq;\n    pvt_approximate_year = 2017,\n    extract = tracking_details,\n)\n\neltype(data_channel)\n@NamedTuple{runtime::Unitful.Quantity{Float64, 𝐓, Unitful.FreeUnits{(s,), 𝐓, nothing}}, sats::Dict{Any, Any}}\n\nsnapshots = collect_data(data_channel)   # a Vector of our named tuples\nfinal = last(snapshots)\n\nfor (signal_id, prn) in sort(collect(keys(final.sats)))\n    s = final.sats[(signal_id, prn)]\n    println(\n        signal_id, \" PRN \", lpad(prn, 2),\n        \"   carrier Doppler \", round(ustrip(u\"Hz\", s.doppler); digits = 1), \" Hz\",\n        \"   code phase \", round(s.code_phase; digits = 1),\n        \"   healthy \", s.healthy,\n    )\nend\nGPSL1CA PRN  5   carrier Doppler -4010.3 Hz   code phase 8395.2   healthy true\nGPSL1CA PRN  7   carrier Doppler -4407.5 Hz   code phase 577.8   healthy true\nGPSL1CA PRN  8   carrier Doppler -1920.2 Hz   code phase 19741.3   healthy true\nGPSL1CA PRN 13   carrier Doppler -341.7 Hz   code phase 14640.9   healthy true\nGPSL1CA PRN 15   carrier Doppler 1358.7 Hz   code phase 9425.6   healthy true\nGPSL1CA PRN 18   carrier Doppler 1866.8 Hz   code phase 17774.3   healthy true\nGPSL1CA PRN 20   carrier Doppler -1386.1 Hz   code phase 11050.4   healthy true\nGPSL1CA PRN 21   carrier Doppler -1918.3 Hz   code phase 18054.0   healthy true\nGPSL1CA PRN 24   carrier Doppler 2422.9 Hz   code phase 19055.9   healthy true\nGPSL1CA PRN 28   carrier Doppler -964.3 Hz   code phase 10933.8   healthy true\nGPSL1CA PRN 30   carrier Doppler -3622.5 Hz   code phase 9624.5   healthy true","title":"Example"},{"category":"section","location":"custom_output/#Persisting-and-plotting","page":"Custom Receiver Output","text":"Because the channel simply carries whatever extract returns, the usual consumers work without change:\n\ncollect_data gathers the run into a Vector (as above) for analysis.\nsave_data writes it to a JLD2 file.\nget_gui_data_channel + gui expect the default ReceiverDataOfInterest, so keep the default extract when you want the live GUI.","title":"Persisting and plotting"},{"category":"section","location":"custom_output/#Defining-a-typed-payload","page":"Custom Receiver Output","text":"For a long-lived pipeline you may prefer a concrete struct over a named tuple — it documents the payload and keeps the channel element type explicit:\n\nstruct DopplerSnapshot\n    runtime::typeof(1.0u\"s\")\n    dopplers::Dict{Tuple{Symbol,Int},typeof(1.0u\"Hz\")}\nend\n\nfunction doppler_only(receiver_state)\n    track_state = receiver_state.track_state\n    dopplers = Dict(\n        (group_key, get_prn(sat_state)) => get_carrier_doppler(sat_state)\n        for group_key in keys(receiver_state.receiver_sat_states)\n        for sat_state in get_sat_states(track_state, group_key)\n    )\n    return DopplerSnapshot(receiver_state.runtime, dopplers)\nend\n\nPassing extract = doppler_only then gives you a channel — and a collect_data result — of DopplerSnapshot.","title":"Defining a typed payload"},{"category":"section","location":"#GNSSReceiver.jl","page":"Home","text":"A software-defined GNSS (Global Navigation Satellite System) receiver written in pure Julia. GNSSReceiver.jl takes raw radio-frequency samples — streamed live from an SoapySDR device or replayed from a file — and runs the full receiver pipeline over one or several constellations and RF bands:\n\nsamples ─▶ acquisition ─▶ tracking ─▶ navigation-bit decoding ─▶ PVT solution\n\nThe result is a Position/Velocity/Time fix that you can watch update live in a terminal GUI, persist to disk, or post-process in Julia.","title":"GNSSReceiver.jl"},{"category":"section","location":"#Features","page":"Home","text":"End-to-end acquire → track → decode → PVT pipeline over a lock-free sample channel.\nMulti-constellation, multi-band: GPS (L1 C/A, L1C, L2C, L5), Galileo (E1, E5a, E5b, E6-B) and BeiDou (B1I, B3I, B1C, B2a, B2b), on their GPST/GST/BDT time systems. Pass a tuple of systems that share one RF band to fuse them (e.g. (GPSL1CA(), GalileoE1B())), or a tuple of measurement channels and system groups to fuse several bands into one solution with per-constellation clock biases and per-band inter-frequency biases.\nPilot + data combined tracking via CombinedSignal: range on the dataless pilot, decode the navigation message from the data component (e.g. Galileo E1B/E1C, GPS L5I/L5Q, BeiDou B1C-D/B1C-P).\nLive reception from any SoapySDR device (RTL-SDR, LimeSDR, BladeRF, USRP, …).\nOffline replay from files, including raw 8-bit offset-binary I/Q recordings.\nMulti-antenna processing with eigen-beamforming.\nA live, interactive terminal dashboard showing per-satellite carrier-to-noise ratios, a satellite sky plot, and the computed position on an OpenStreetMap map.\nConfigurable acquisition cadence, lock-detection and PVT parameters — see Acquisition & Tracking Parameters.","title":"Features"},{"category":"section","location":"#Installation","page":"Home","text":"using Pkg\nPkg.add(\"GNSSReceiver\")\n\nOr from the Julia REPL:\n\njulia> ]\npkg> add GNSSReceiver\n\nTo receive from a real SDR you also need a SoapySDR driver for your device, e.g. SoapyRTLSDR_jll for an RTL-SDR dongle.","title":"Installation"},{"category":"section","location":"#Quick-start","page":"Home","text":"Receive live from an SDR and show the GUI (see Getting Started for the full walk-through):\n\nusing SoapyRTLSDR_jll          # your device's SoapySDR driver\nusing GNSSReceiver, GNSSSignals, Tracking, SoapySDR, Unitful\n\ngnss_receiver_gui(;\n    system = GPSL1CA(),\n    sampling_freq = 2e6u\"Hz\",\n    chunk_time = 4u\"ms\",\n    run_time = 40u\"s\",\n    num_ants = Tracking.NumAnts(1),\n    dev_args = first(Devices()),\n)\n\nDon't have an SDR handy? The Worked Example (Real Data) page downloads a public GNSS recording and computes a real position fix — and it runs automatically every time these docs are built.","title":"Quick start"},{"category":"section","location":"#Where-to-go-next","page":"Home","text":"Getting Started — the building blocks (receive, get_gui_data_channel, save_data) and how to feed the receiver from a device or a file.\nAcquisition & Tracking Parameters — every knob you can turn (acquisition cadence, PRN search, lock thresholds, correlator, PVT options, …) and how to set it.\nWorked Example (Real Data) — a complete, runnable end-to-end fix on real data.\nCustom Receiver Output — emit your own per-chunk payload (raw carrier Doppler, code phase, decoded data, …) instead of the default summary.\nGraphical User Interface — the live terminal GUI.\nAPI Reference — every exported function.","title":"Where to go next"}]
}
